# iOS Feature Parity

This matrix answers a different question from
[`ios-single-repo-migration.md`](ios-single-repo-migration.md): not only whether
logic is shared, but whether the iOS product contains every intended macOS and
Roma Just Talk feature.

## Audit Basis

- Audited macOS/iOS source: `bb48ed94a88571eff08b98f8b8aeb6c39ca5b667`.
- Fork comparison: `origin/main` at `ad022c62`, merge base `0df2a9ab`.
- Exact-HEAD CI: [single-repo gates](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29111970026) and [macOS tests](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29111974233).
- CI proves shared checks and compilation. It does not prove a user workflow.
- `source-present` means a reachable implementation was found, but runtime proof
  is still required unless the row names separate runtime evidence.

## Classification

- `shared/iOS`: portable product behavior intended on iOS. A platform shell may
  differ, but the user outcome must exist.
- `macOS-only`: the exact behavior depends on a macOS-only system capability.
- `not-applicable`: an iOS equivalent would not serve the same workflow.
- `decision`: technically possible or partially substitutable, but product scope
  must be chosen explicitly. A decision row blocks completion.

## Recording And Entry

| ID | Capability | macOS evidence | iOS state and implementation | Class | Verification |
|---|---|---|---|---|---|
| R01 | In-app start, stop, cancel, meter, and duration | [`RecorderUIManager`](../VoiceInk/Transcription/Engine/RecorderUIManager.swift), [`TranscriptionPipeline`](../VoiceInk/Transcription/Engine/TranscriptionPipeline.swift) | `implemented, runtime proof pending`: [`RecordingManager`](../iOS/VoiceInk-ios/RecordingManager.swift) owns permission, capture, meter/duration, cancel/stop, streaming/batch finalization, persistence, and alerts while [`RecordingSheetView`](../iOS/VoiceInk-ios/RecordingSheetView.swift) exposes the active controls | shared/iOS | Shared flow/coordinator tests and exact-HEAD builds are required; permission, live capture, meter, duration, persistence, success, cancel, and failure still need simulator/device proof. |
| R02 | Keyboard entry starts/stops dictation | Global shortcut path plus [`VoiceInkEngine`](../VoiceInk/Transcription/Engine/VoiceInkEngine.swift) | `implemented, runtime proof pending`: [`KeyboardViewController`](../iOS/VoiceInkKeyboard/KeyboardViewController.swift) opens `romajusttalk://record`, coordinates the App Group request/state lifecycle, and requests stop through the app-owned recorder | shared/iOS | Exact-HEAD tests cover request/state coordination; opening the app, repeated start/stop, and host-keyboard round trips still need simulator/device proof. |
| R03 | Dictated text returns to the originating field | [`TranscriptionPipeline`](../VoiceInk/Transcription/Engine/TranscriptionPipeline.swift), [`CursorPaster`](../VoiceInk/Paste/CursorPaster.swift) | `implemented, runtime-proven`: [`KeyboardDictationExchange`](../iOS/Shared/KeyboardDictationExchange.swift) binds one App Group result to the originating document and its bounded cursor context; [`KeyboardViewController`](../iOS/VoiceInkKeyboard/KeyboardViewController.swift) automatically inserts a context match once, while an empty ambiguous field requires an explicit Insert Dictation tap | shared/iOS | [Exact-HEAD CI `29522248593`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29522248593) built SHA `26801436`; [exact-artifact stage `29523172143`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29523172143) proved keyboard-to-app request handoff, automatic non-empty-field return after the document UUID changed, explicit empty-field confirmation, one-time insertion, and exchange clearing. The transcription result was deterministically seeded because the staged app had no configured mode; recording/background completion remains tracked by R02 and R06. |
| R04 | Microphone permission and denied-state recovery | [`PermissionsView`](../VoiceInk/Views/PermissionsView.swift), onboarding permission flow | `implemented, partially runtime-proven`: onboarding and Settings expose shared permission-state guidance, refresh after returning from system Settings, and route recording through request/denial/retry handling | shared/iOS | [Exact-artifact stage `29534751513`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29534751513) proved the onboarding system prompt, denial, denied-state relaunch, Open Settings routing, and refresh to ready after a system grant. [Exact-HEAD CI `29720065460`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29720065460) built SHA `ee4d9f7a`; [exact-artifact stage `29720434054`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29720434054) proved the dedicated Settings screen's undetermined state, real system prompt, denial, Open Settings routing, and granted-state re-read after relaunch. The iOS 26 Simulator omitted the app's microphone toggle, so grants used `simctl privacy`; real toggle return and successful recording retry still need device proof. Evidence artifacts: `8390706867`, `8452320688`. |
| R05 | Keyboard installation, Full Access, and readiness setup | macOS onboarding proves required shortcut/permission readiness | `implemented, runtime-proven`: onboarding and Settings expose [`KeyboardSetupView`](../iOS/VoiceInk-ios/KeyboardSetupView.swift); the extension reports a fresh activation and its `hasFullAccess` result through [`AppGroupCoordinator`](../iOS/Shared/AppGroupCoordinator.swift), while the keyboard blocks dictation without Full Access | shared/iOS | [Exact-HEAD CI `29527542165`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29527542165) built SHA `d9f71a1e`; [exact-artifact stage `29528172615`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29528172615) proved fresh onboarding, keyboard installation and selection, the blocked Full Access-off state, recovery through system Settings, enabled readiness, and fresh-session re-verification. Evidence artifact: `8388286951`. |
| R06 | Completion after leaving the app | macOS process owns the whole recording/transcription pipeline | `implemented, partially runtime-proven`: [`IOSTranscriptionTaskCoordinator`](../iOS/VoiceInk-ios/IOSTranscriptionTaskCoordinator.swift) owns initial/retry work, requests balanced UIKit background execution, delivers keyboard outcomes once, turns expiration into a retryable failure, and recovers orphaned pending records after relaunch | shared/iOS | [Exact-artifact stage `29531177552`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29531177552) proved local Whisper completion after leaving the app, balanced UIKit background-task completion after 23 seconds, UIKit expiration after 41 seconds, and a persisted saved-audio retry state. [Exact-HEAD CI `29721744136`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29721744136) built SHA `76fe4347`; [exact-artifact stage `29724530201`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29724530201) sent the app Home during delayed OpenAI-compatible Custom Cloud STT plus Ollama enhancement, received both responses while backgrounded, preserved PID `2476`, ended the named UIKit task after 13 seconds without expiration, persisted raw and enhanced completed text, and displayed the enhanced result after return. The deterministic local endpoints prove iOS remote/enhancement transport and background coordination, not live third-party compatibility; real providers and keyboard result return remain pending. Evidence artifacts: `8389251847`, `8453848821`. |
| R07 | Cancel active STT or enhancement | [`VoiceInkEngine`](../VoiceInk/Transcription/Engine/VoiceInkEngine.swift) tracks and cancels processing | `implemented, runtime-proven`: note detail can cancel coordinator-owned work; shared retranscription/post-processing propagates cancellation, and iOS local Whisper uses whisper.cpp's abort callback before persisting a coherent canceled record | shared/iOS | The [exact-artifact stage `29531177552`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29531177552) canceled an active 61:40 local Whisper run from note detail, ended its background task, persisted `canceled`, and preserved the canceled history state after relaunch. [Exact-HEAD CI `29725299015`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29725299015) built SHA `c2213fec`; [exact-artifact stage `29726656892`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29726656892) canceled delayed Custom Cloud STT from active note detail and observed the client disconnect after 6 seconds, then completed STT and canceled delayed Ollama enhancement with a client disconnect after 7 seconds. Both named UIKit tasks ended without expiration, two canceled rows persisted, and canceled history remained visible. Deterministic local endpoints prove iOS cancellation propagation, not live third-party compatibility. Evidence artifacts: `8389251847`, `8454614472`. |
| R08 | Manual audio-file import and transcription queue | [`AudioTranscribeView`](../VoiceInk/Views/AudioTranscribeView.swift) | `implemented, partially runtime-proven`: [`AudioImportView`](../iOS/VoiceInk-ios/AudioImportView.swift) and [`IOSAudioImportManager`](../iOS/VoiceInk-ios/IOSAudioImportManager.swift) accept multiple picker or external-open audio/video files, prepare 16 kHz mono WAV copies, process them sequentially through the selected mode and existing background-safe transcription owner, and expose cancel, retry, persistent detail, and transcript sharing | shared/iOS | [Exact-HEAD CI `29506288911`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29506288911) built SHA `59de023e`; [exact-artifact stage `29507021210`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29507021210) proved external-open intake, canonical 16 kHz mono WAV preparation, local Whisper base transcription, completed-note presentation, transcript detail with the exact visible 15-word phrase, and isolated detail navigation without opening Share. [Exact-artifact stage `29724530201`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29724530201) proved deterministic OpenAI-compatible remote transcription plus enhancement completion through external-open intake; [stage `29726656892`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29726656892) proved active remote STT and enhancement cancellation. [Exact-HEAD CI `29727722553`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29727722553) built SHA `05e271c5`; [exact-artifact stage `29728421568`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29728421568) presented the system Share sheet from a completed import with the exact transcript payload and visible Reminders, More, Copy, and Save to Files actions without invoking a destination, terminated PID `2132` with no remaining process, relaunched as PID `2808`, and reopened the completed transcript from persisted history and detail. Live-provider compatibility, picker and Files-share intake, long/multiple files, retry, and share-destination handoff still need simulator/device proof. Evidence artifacts: `8453848821`, `8454614472`, `8455440601`. |
| R09 | Recorder audio pre-roll | [`Recorder`](../VoiceInk/Recorder.swift), [`CoreAudioRecorder`](../VoiceInk/CoreAudioRecorder.swift) | `implemented, device runtime proof pending`: after onboarding completes in an active scene and microphone permission is granted, [`AudioRecorder`](../iOS/VoiceInk-ios/AudioRecorder.swift) warms capture; recording start also establishes capture when needed, snapshots up to three seconds from the shared PCM ring into the saved file, and queues that audio before live streaming chunks; when idle, inactive/background teardown clears the ring. Speculative STT Preload is removed and is not part of R09. | shared/iOS | Shared ring ordering/resize tests and exact-HEAD builds are required; device-proof the available lead-in after warm-up, first permission grant, foreground recovery, streaming/batch ordering, and absence of stale audio across background gaps. |
| R10 | Active streaming and live partial transcript | [`StreamingTranscriptionService`](../VoiceInk/Transcription/Streaming/StreamingTranscriptionService.swift), [`MiniRecorderView`](../VoiceInk/Views/Recorder/MiniRecorderView.swift) | `implemented, runtime proof pending`: streaming-capable fixed cloud models expose a per-model control; [`IOSStreamingTranscriptionService`](../iOS/VoiceInk-ios/IOSStreamingTranscriptionService.swift) drives the shared provider dependency while [`AudioRecorder`](../iOS/VoiceInk-ios/AudioRecorder.swift) writes the saved WAV and sends ordered 16 kHz mono PCM; the recording sheet shows partials, committed text uses the shared cleanup/enhancement pipeline, and provider failures fall back to the saved-file batch path | shared/iOS | Route, partial/final, provider-error, shared-finalization, exact-HEAD build, and macOS preservation tests are required; each provider, long audio, network loss, final timeout, enhancement, cancellation, and saved-file fallback still need simulator/device proof. |
| R11 | Batch Whisper VAD | [`ModelSettingsView`](../VoiceInk/Views/ModelSettingsView.swift) | `implemented, runtime proof pending`: iOS local-model Settings exposes the shared VAD preference; the bundled Silero path and saved value feed the same [`VoiceInkWhisperRuntimeConfiguration`](../VoiceInkCore/Sources/VoiceInkCore/LocalWhisperTranscriptionFlow.swift) used by macOS and iOS whisper.cpp shells | shared/iOS | Preference-to-runtime parameter tests and exact-HEAD builds are required; enabled/disabled speech, silence, segmentation, and latency still need device proof. |
| R12 | Model prewarm and retained local context | [`ModelPrewarmService`](../VoiceInk/Services/ModelPrewarmService.swift) | `implemented, runtime proof pending`: [`IOSRetainedContextCache`](../iOS/VoiceInk-ios/IOSRetainedContextCache.swift) coalesces model loads and retains one context across local runs; [`IOSModelPrewarmService`](../iOS/VoiceInk-ios/IOSModelPrewarmService.swift) uses the shared preference/delay/eligibility policy on launch and foreground activation, cancels before recording, and releases on model deletion or memory warning | shared/iOS | Cache reuse/replacement/release, preference policy, exact-HEAD builds, and focused tests are required; measure cold/prewarmed/repeat latency, cancellation, memory pressure, model switching, and deletion on device. |
| R13 | Choose mode/prompt during recording | [`MiniRecorderShortcutManager`](../VoiceInk/Shortcuts/MiniRecorderShortcutManager.swift) | `implemented, runtime proof pending`: the iOS recording sheet selects the active mode and an optional reusable prompt override; captured run settings apply both to the same recording and clear the prompt override after the run | shared/iOS | Focused mode/prompt snapshot tests and exact-HEAD builds are required; same-run selection, streaming/batch paths, cancel, and next-recording reset still need simulator/device proof. |
| R14 | Start/stop/custom recording feedback | [`SoundManager`](../VoiceInk/SoundManager.swift) | `implemented, runtime proof pending`: iOS Settings exposes independent haptic and sound toggles, silent mode, seven shared built-in choices, three-second validated custom start/stop imports, preview, and runtime cues that finish before microphone capture starts | shared/iOS | Preference/selection tests and exact-HEAD builds are required; speaker/Bluetooth routing, media coexistence, cue audibility, silent mode, custom import, cancel-during-start, and no cue leakage into captured audio need device proof. |
| R15 | Input-device selection and priority | [`AudioDeviceManager`](../VoiceInk/Services/AudioDeviceManager.swift), [`AudioInputSettingsView`](../VoiceInk/Views/Settings/AudioInputSettingsView.swift) | `implemented, device runtime proof pending`: iOS defaults to system-managed routing and exposes [`IOSAudioInputSettingsView`](../iOS/VoiceInk-ios/IOSAudioInputSettingsView.swift) to opt into a persisted preferred input; policy attempts that input when available, otherwise leaves route selection to iOS; recorder serialization defers preference/reconnect-driven capture restarts until recording stops, while system routing remains OS-managed; shared source tests preserve the existing macOS custom-device default | shared/iOS | Shared platform-default/fallback tests and exact-HEAD builds are required; device-proof built-in, wired, Bluetooth, disconnect/reconnect, idle switching, and deferred switching after an active recording stops. |
| R16 | Global hotkey and middle-click triggers | [`RecordingShortcutManager`](../VoiceInk/Shortcuts/RecordingShortcutManager.swift) | Keyboard extension is the iOS entry adaptation | macOS-only | macOS event taps/global input do not exist for normal iOS apps. |
| R17 | Toggle, push-to-talk, hybrid, and Special Shift modes | [`UserDefaultsPreferences`](../VoiceInkCore/Sources/VoiceInkCore/UserDefaultsPreferences.swift), shortcut managers | iOS keyboard exposes a tap-toggle interaction | not-applicable | These modes describe a physical global-key lifecycle; iOS needs its own keyboard-button interaction, not mirrored key events. |
| R18 | System mute and targeted media pause/resume | [`MediaController`](../VoiceInk/MediaController.swift), [`PlaybackController`](../VoiceInk/PlaybackController.swift) | iOS uses `AVAudioSession` mixing/routing behavior | macOS-only | Exact system-wide media control is not available to a normal iOS app. |
| R19 | None, notch, and mini recorder windows | [`RecordingStatePolicy`](../VoiceInkCore/Sources/VoiceInkCore/RecordingStatePolicy.swift), recorder views | iOS uses an in-app recording sheet | macOS-only | `NSPanel`, notch placement, and floating desktop windows are macOS-only. |
| R20 | App Intents / Shortcuts start, stop, and dismiss | [`AppShortcuts`](../VoiceInk/AppIntents/AppShortcuts.swift) | `implemented, runtime proof pending`: [`IOSRecordingAppIntents`](../iOS/VoiceInk-ios/IOSRecordingAppIntents.swift) exposes start, stop, and cancel App Shortcuts; one durable request store hands each invocation to the foreground app, where the existing recording manager owns permission, capture, transcription, and discard behavior | shared/iOS | Request/recording-state policy tests and exact-HEAD builds are required; prove Shortcuts discovery, cold/warm invocation, permission grant/denial, stop-to-transcription, cancel/discard, and user-visible errors on simulator/device. |
| R21 | Cursor-aware capitalization, trailing space, clipboard restore, and auto-send | [`CursorPaster`](../VoiceInk/Paste/CursorPaster.swift), [`CursorTextContextReader`](../VoiceInk/Services/CursorTextContextReader.swift) | `implemented, runtime proof pending` for the iOS-applicable scope: keyboard delivery applies shared trailing-space and cursor-context capitalization policies before one-time `textDocumentProxy` insertion, captures the optional auto-send preference with the completed App Group result, and inserts exactly one Return afterward when enabled; clipboard restoration is not applicable because delivery does not mutate the clipboard | shared/iOS for field-aware formatting and auto-send; not-applicable for clipboard restoration when insertion uses `textDocumentProxy` | Shared preference/plan and exchange tests plus exact-HEAD builds are required; prove sentence/mid-sentence insertion, spacing, lowercasing override, compatible and multiline fields, one-time Return, and no unrelated clipboard mutation on simulator/device. |

## Transcription, Models, And Enhancement

| ID | Capability | macOS evidence | iOS state and implementation | Class | Verification |
|---|---|---|---|---|---|
| M01 | Bundled/local Whisper model management | [`ModelManagementView`](../VoiceInk/Views/AI%20Models/ModelManagementView.swift) | `implemented, runtime proof pending`: [`LocalModelManagementView`](../iOS/VoiceInk-ios/LocalModelManagementView.swift) and [`LocalModelManager`](../iOS/VoiceInk-ios/LocalModelManager.swift) expose bundled download, imported-model intake, progress/cancel, selection, deletion, refresh, retained-context prewarm, and local transcription | shared/iOS | Focused model/import/cache tests and exact-HEAD builds are required; download/cancel/delete, relaunch, offline transcription, model switching, and failure recovery still need device proof. |
| M02 | Fixed cloud batch transcription providers | [`TranscriptionModelCatalog`](../VoiceInkCore/Sources/VoiceInkCore/TranscriptionModelCatalog.swift) | `implemented, runtime proof pending`: iOS mode selection uses the shared provider/model catalog, credential readiness, remote request builders/transports, empty-output policy, cleanup, retry/failure presentation, and transcription-run processor | shared/iOS | Shared request/transport/run tests and exact-HEAD builds are required; every selectable provider needs real credential, request, success, empty output, retry, cancellation, and failure proof on device. |
| M03 | Native Apple transcription | [`NativeAppleTranscriptionService`](../VoiceInk/Transcription/Native/NativeAppleTranscriptionService.swift) | `implemented, runtime proof pending`: iOS 26+ modes expose the shared Native Apple provider/model and BCP-47 languages; [`IOSNativeAppleTranscriptionService`](../iOS/VoiceInk-ios/IOSNativeAppleTranscriptionService.swift) runs recorded files through SpeechAnalyzer, while Settings checks and downloads the selected language asset through [`IOSNativeAppleLanguageAssetView`](../iOS/VoiceInk-ios/IOSNativeAppleLanguageAssetView.swift) | shared/iOS | Provider gating/routing, model/language, invalid-model, platform-error tests, and exact-HEAD builds are required; supported/unsupported locale, asset download, offline transcription, cancellation, timeout, empty audio, and user-visible retry still need iOS 26 device proof. |
| M04 | FluidAudio / Parakeet local transcription | [`FluidAudioTranscriptionService`](../VoiceInk/Transcription/FluidAudio/FluidAudioTranscriptionService.swift) | `implemented, runtime proof pending`: iOS local-model management exposes shared Parakeet V2/V3 metadata with the same download/retry/stall/deletion manager; the in-repo FluidAudio package powers recorded-file transcription, agreement streaming, language hints, VAD, cancellation, and prewarming through separate iOS execution shells | shared/iOS | Shared routing and iOS lifecycle tests plus exact-HEAD builds are required; model download/delete, batch and live output, V2 English, V3 multilingual, memory warning, cancellation, and offline/error behavior still need device proof. |
| M05 | Import a local Whisper model | macOS local-model import in model management | `implemented, runtime proof pending`: iOS model management imports security-scoped `.bin` files through [`IOSLocalWhisperModelImporter`](../iOS/VoiceInk-ios/IOSLocalWhisperModelImporter.swift), persists normalized copies in the existing model directory, lists and deletes imported models, exposes them in mode selection, and preserves the selected imported name through shared run configuration into the existing Whisper service | shared/iOS | Import/copy, duplicate, unsupported-file, selection-to-runtime, exact-HEAD build, and focused tests are required; Files picker access, large model copy, successful transcription, relaunch persistence, deletion while selected, and user-visible failures still need simulator/device proof. |
| M06 | Cartesia and custom cloud STT endpoints | [`CustomCloudModelManager`](../VoiceInk/Transcription/Cloud/CustomCloudModelManager.swift), shared catalog | `implemented, runtime proof pending`: Cartesia exposes verified credentials, model selection, and forced streaming; iOS custom endpoints support add/edit/delete, Keychain-backed per-model keys, dynamic mode selection, recorded-file routing through the shared OpenAI-compatible request policy, reset cleanup, and visible validation/runtime failures | shared/iOS | Shared provider/mode/custom-request policy with iOS LLMkit streaming, persistence/Keychain, SwiftUI CRUD, and batch execution shells; exact-HEAD builds and focused tests are required, while real Cartesia and custom endpoint success/failure still need device proof. |
| M07 | Language selection and request normalization | [`ModelSettingsView`](../VoiceInk/Views/ModelSettingsView.swift) | `implemented, runtime proof pending`: the iOS language picker persists the shared language code, repairs unsupported mode changes, applies per-language local prompts, and feeds shared local/remote request normalization | shared/iOS | Shared language/prompt/request tests and exact-HEAD builds are required; local and every remote provider still need non-English runtime samples and unsupported-language fallback proof. |
| M08 | Per-language Whisper prompt, trailing space, VAD, prewarm, preview controls | [`ModelSettingsView`](../VoiceInk/Views/ModelSettingsView.swift) | `implemented, runtime proof pending`: iOS edits and resolves the selected language's shared Whisper prompt for each run, applies the shared trailing-space preference to keyboard delivery, and exposes shared live-transcript, batch VAD, and model-prewarm controls | shared/iOS | Focused prompt and keyboard-delivery tests plus exact-HEAD builds are required; persistence and observable runtime effect for each setting still need simulator/device proof. |
| M09 | Vocabulary, replacements, and cleanup | [`DictionarySettingsView`](../VoiceInk/Views/Dictionary/DictionarySettingsView.swift) | `implemented, runtime proof pending`: iOS can add/delete vocabulary, add/edit/delete replacement rules, and configure punctuation, paragraphs, lowercase, and fillers through shared validation, ordering, application, and persistence policies; macOS Quick Add remains a desktop-only global-shortcut panel | shared/iOS | Shared edit/delete/order/application tests and exact-HEAD builds are required; settings persistence and observable cleanup/replacement behavior still need simulator/device proof. |
| M10 | Mode CRUD with transcription and enhancement selection | [`PowerModeConfigView`](../VoiceInk/PowerMode/PowerModeConfigView.swift) | `implemented, runtime proof pending`: [`ModeConfigurationView`](../iOS/VoiceInk-ios/ModeConfigurationView.swift) and [`AppSettings`](../iOS/VoiceInk-ios/AppSettings.swift) add, edit, delete, repair, persist, and select manual modes whose captured run settings drive transcription and enhancement | shared/iOS | Shared mode mutation/repair tests and exact-HEAD builds are required; CRUD persistence, relaunch, recording-time execution, active-mode deletion, and unavailable-provider repair still need simulator/device proof. |
| M11 | Fixed AI enhancement providers | [`AIProviderCatalog`](../VoiceInkCore/Sources/VoiceInkCore/AIProviderCatalog.swift) | `implemented, runtime proof pending`: iOS mode post-processing exposes the fixed shared Groq, OpenAI, Cerebras, Gemini, and Mistral providers when their verified credentials are ready, with shared model catalogs and request routing | shared/iOS | Catalog/readiness/route tests and exact-HEAD builds are required; real enhancement success, provider errors, and fallback output still need device proof for every provider/model. |
| M12 | Anthropic, OpenRouter, Ollama, and generic custom enhancement | macOS provider settings and [`AIService`](../VoiceInk/Services/AIEnhancement/AIService.swift) | `implemented, runtime proof pending`: iOS supports Anthropic, OpenRouter, generic custom, and Ollama configuration/readiness, shared or dynamic model selection, provider-correct request routing, local-network permission disclosure, and the existing filtered fallback path | shared/iOS | Exact-HEAD builds and focused configuration/route tests are required; real provider success/failure, remote model refresh, Ollama discovery, local-network denial, timeout/retry, and output fallback still need device proof. |
| M13 | Local CLI enhancement | [`LocalCLIService`](../VoiceInk/Services/AIEnhancement/LocalCLIService.swift) | No iOS process/shell execution equivalent | macOS-only | Arbitrary `/bin/zsh` command execution is unavailable to iOS apps. |
| M14 | Reusable prompt library, reorder, trigger words, per-recording choice | [`EnhancementSettingsView`](../VoiceInk/Views/EnhancementSettingsView.swift), [`AIEnhancementService`](../VoiceInk/Services/AIEnhancement/AIEnhancementService.swift) | `implemented, runtime proof pending`: iOS Settings exposes native prompt add/edit/delete/reorder controls, shared predefined/template content and trigger-word editing, mode prompt selection, and a recording-sheet override; captured run settings apply trigger precedence, strip matched commands from the enhancement transcript, preserve fallback text, and record the chosen prompt name | shared/iOS | Shared CRUD/order/trigger/run tests and exact-HEAD builds are required; persistence, editor gestures, mode/default/override selection, trigger precedence, real provider requests, relaunch, and deletion repair still need simulator/device proof. |
| M15 | Clipboard, selected-text, screen, and surrounding-text context | [`AIEnhancementService`](../VoiceInk/Services/AIEnhancement/AIEnhancementService.swift) | `implemented, runtime proof pending` for the iOS-applicable scope: the keyboard captures at most 240 characters before the cursor, the app consumes and clears that shared exchange field, captured batch/stream runs add a tagged surrounding-text section to enhancement prompts, and final delivery reads the current field context for capitalization. The keyboard shell separately captures the current pasteboard only when the user opens History, then keeps searchable text/link/image entries in the App Group with deduplication, pinning, deletion, 30-day retention, and direct text insertion; passive system-wide clipboard/selection/screen capture remains macOS-only. | shared/iOS for keyboard surrounding text and explicit clipboard-history capture; macOS-only for passive system-wide screen/selection/clipboard capture | Shared bounds/prompt/capitalization, exchange lifecycle, and clipboard-store tests plus exact-HEAD builds are required; prove real host-field capture, clipboard swipe/search/filter/pin/delete/clear, text/link insertion, image copy-back, Full Access-off behavior, background round trip, provider request inspection, sentence/mid-sentence insertion, relaunch/expiry, and no unintended clipboard capture on simulator/device. |
| M16 | Automatic Power Mode by foreground app/browser URL | [`ActiveWindowService`](../VoiceInk/PowerMode/ActiveWindowService.swift) | Manual iOS modes are present; no system-wide app/URL inspection | macOS-only | Normal iOS apps cannot inspect another foreground app or browser URL globally. |
| M17 | Provider API-key entry, verification, and Keychain storage | [`APIKeyManagementView`](../VoiceInk/Views/AI%20Models/APIKeyManagementView.swift) | `implemented, runtime proof pending`: [`APIKeysView`](../iOS/VoiceInk-ios/APIKeysView.swift) and [`ProviderAPIKeyView`](../iOS/VoiceInk-ios/ProviderAPIKeyView.swift) expose provider-specific entry, verification progress/results, Keychain save/change/delete, readiness refresh, and storage/verification failure feedback | shared/iOS | Shared verification/form tests and exact-HEAD builds are required; real save/change/delete, provider verification, relaunch, environment fallback where applicable, and failed-Keychain behavior still need device proof. |
| M18 | Enhancement context, minimum-length, timeout, retry, and fallback controls | macOS enhancement settings and [`AIEnhancementService`](../VoiceInk/Services/AIEnhancement/AIEnhancementService.swift) | `implemented, runtime proof pending`: iOS Settings exposes keyboard-context, short-transcript threshold, timeout, and retry controls; captured run settings apply them to batch, streaming-finalization, and history re-enhancement, retry timed-out requests up to three attempts, and keep the original transcript with an explicit error after final failure | shared/iOS | Focused preference/execution/fallback tests and exact-HEAD builds are required; prove context on/off, skip thresholds, timeout/retry timing, provider cancellation, fallback copy, and final user-visible errors on simulator/device. |

## History, Metrics, And Data

| ID | Capability | macOS evidence | iOS state and implementation | Class | Verification |
|---|---|---|---|---|---|
| H01 | Searchable transcription history | [`InlineHistoryView`](../VoiceInk/Views/History/InlineHistoryView.swift) | `implemented, runtime proof pending`: [`NotesListView`](../iOS/VoiceInk-ios/NotesListView.swift) persists timestamped records, searches raw/enhanced text through shared snapshot policy, navigates to detail, and deletes rows plus recording files individually or in bulk | shared/iOS | Shared search/selection/deletion tests and exact-HEAD builds are required; persistence, relaunch, accented search, deletion, missing files, and large-history behavior still need simulator/device proof. |
| H02 | Transcript detail, copy, audio playback, seek, and speed | macOS history/audio player views | `implemented, runtime proof pending`: [`NoteDetailView`](../iOS/VoiceInk-ios/NoteDetailView.swift), [`AudioPlayerView`](../iOS/VoiceInk-ios/AudioPlayerView.swift), and [`AudioPlayer`](../iOS/VoiceInk-ios/AudioPlayer.swift) expose transcript copy/share, metadata, audio play/pause, seek, speed, loading/failure states, and playback-owned audio sessions | shared/iOS | Shared playback timeline/session tests and exact-HEAD builds are required; clipboard/share targets, route changes, interruption, seek/rate, backgrounding, and missing-audio behavior still need device proof. |
| H03 | Retry, retranscribe, and re-enhance | [`LastTranscriptionService`](../VoiceInk/Services/LastTranscriptionService.swift), macOS audio player | `implemented, runtime proof pending`: completed iOS records expose mode/prompt selection, stored-audio retranscription, and transcript-only re-enhancement; settings are captured per action, successful enhancement persists model/prompt metadata, and failure/cancellation preserves the completed record and prior enhancement | shared/iOS | Shared success/failure mutation tests and exact-HEAD builds are required; real provider requests, stored-audio replacement, cancellation, relaunch persistence, missing-audio controls, and prior-result preservation still need simulator/device proof. |
| H04 | Bulk selection/delete, CSV export, and share | [`InlineHistoryView`](../VoiceInk/Views/History/InlineHistoryView.swift) | `implemented, runtime proof pending`: iOS history edit mode supports shared select-all/count policy, destructive-confirmed multi-delete with audio cleanup and active-task cancellation, and a share-sheet CSV file built by the shared exporter | shared/iOS | Shared CSV/deletion tests and exact-HEAD builds are required; filtered selection, select/deselect all, cancel confirmation, file contents/name, Files/AirDrop targets, active-note deletion, and relaunch persistence still need simulator/device proof. |
| H05 | Full metrics and model-performance dashboard | [`MetricsContent`](../VoiceInk/Views/Metrics/MetricsContent.swift) | `implemented, runtime proof pending`: iOS exposes a native dashboard with persisted 7-day/30-day/year/all-time filters, shared time-saved/pace/keystroke cards, totals, empty state, and transcription/enhancement model comparisons derived from stored notes | shared/iOS | Shared snapshot/filter/model tests and exact-HEAD builds are required; filter persistence, live history updates, large datasets, locale formatting, and model-card layout still need simulator/device proof. |
| H06 | Transcript/audio retention and manual cleanup | [`AudioCleanupSettingsView`](../VoiceInk/Views/Settings/AudioCleanupSettingsView.swift) | `implemented, runtime proof pending`: iOS Settings exposes transcript/audio retention controls, preview counts and audio size, confirmed manual cleanup, orphan cleanup, active-note exclusion, and a persisted daily foreground cleanup trigger using shared cutoff/schedule/candidate policy | shared/iOS | Shared schedule/candidate/orphan tests and exact-HEAD builds are required; retention boundaries, active processing, missing/deletion-failure files, relaunch scheduling, large histories, preview refresh, and disk-space recovery still need simulator/device proof. |
| H07 | Selective settings backup/export/import | macOS settings import/export services | `implemented, runtime proof pending`: iOS Settings exports and imports a portable JSON file with independently selectable general settings, modes, custom prompts, dictionary entries, and custom model definitions; API keys and user data have no wire fields, legacy files without a version decode with a fallback, and the only fallible storage commit runs before live settings mutation | shared/iOS | Codec/selective-scope/old-payload/secrets/failure-boundary tests and exact-HEAD builds are required; Files/AirDrop round trips, large payloads, model-key reconfiguration, relaunch persistence, and malformed or unwritable imports still need simulator/device proof. |
| H08 | Diagnostic log/support bundle export | [`DiagnosticsSettingsView`](../VoiceInk/Views/Settings/DiagnosticsSettingsView.swift) | `implemented, runtime proof pending`: iOS Settings prepares a shareable current-process unified-log bundle for a selected 15-minute/hour/day/session range, includes compact app/device/runtime facts, redacts credential-like values, email addresses, query secrets, and home-directory paths, and emits an explicit empty-range marker | shared/iOS | Shared range/redaction/content tests and exact-HEAD builds are required; real log availability, large ranges, private-field rendering, empty/failure states, share targets, and support usefulness still need simulator/device proof. |
| H09 | Cross-device persistence/sync | macOS CloudKit-backed dictionary and local history | `partial`: iOS notes/preferences persist locally; no dictionary/history sync | decision | Choose local-only versus CloudKit scope and conflict policy before implementation. |

## Onboarding, Settings, And Product Shell

| ID | Capability | macOS evidence | iOS state and implementation | Class | Verification |
|---|---|---|---|---|---|
| S01 | Resumable onboarding with permission, model, tutorial, skip, and reset | macOS onboarding views and shared presentation/state | `implemented, partially runtime-proven`: [`OnboardingView`](../iOS/VoiceInk-ios/OnboardingView.swift) persists each step and includes permission, model, keyboard, and interactive recording/transcription tutorial flows with skips; shipping Settings exposes the shared reset plan with destructive confirmation | shared/iOS | [Exact-HEAD CI `29534115798`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29534115798) built SHA `2e267dfa`; [exact-artifact stage `29534751513`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29534751513) proved fresh Welcome, kill/relaunch resume at microphone, model, keyboard, and tutorial steps, keyboard/tutorial skip, completion persistence, destructive reset confirmation, and relaunch into fresh onboarding. It also exposed a zero-format AVAudioEngine crash after granted completion; [exact-HEAD CI `29536694597`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29536694597) built fix SHA `c4961f01`, and [exact-artifact stage `29537248501`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29537248501) proved the same no-input route now reports a recoverable pre-roll failure while the main screen remains alive. Tutorial recording success/failure still needs device proof. Evidence artifacts: `8390706867`, `8391099150`. |
| S02 | Dedicated permission status and recovery screen | [`PermissionsView`](../VoiceInk/Views/PermissionsView.swift) | `implemented, partially runtime-proven`: [`IOSMicrophonePermissionView`](../iOS/VoiceInk-ios/IOSMicrophonePermissionView.swift) is reachable from Settings, requests access, opens app Settings after denial, refreshes on return, and shares the same AVAudioSession adapter as recording; [`KeyboardSetupView`](../iOS/VoiceInk-ios/KeyboardSetupView.swift) re-verifies keyboard activation/Full Access | shared/iOS | R05's [exact-artifact stage `29528172615`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29528172615) proved keyboard readiness and Settings recovery. [Exact-artifact stage `29720434054`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29720434054) proved the shipping Microphone Access row and screen, undetermined guidance, the real permission prompt, denied guidance, Open Settings routing, and granted-state re-read after relaunch. The iOS 26 Simulator omitted the app's microphone toggle and `simctl privacy` restarted the app, so real-toggle and same-process return remain device proof. Evidence artifact: `8452320688`. |
| S03 | Help, support, common issues, and announcements | macOS general settings, support policy, announcement manager | `implemented, runtime proof pending`: iOS Settings exposes shared help resources, Common Issues, support email with fallback, a share sheet, the shared announcement preference, proactive banners, manual refresh, dismissal persistence, and retry/error states | shared/iOS | Store lifecycle tests and exact-HEAD builds are required; link/mail/share presentation plus live fetch, dismissal, offline, retry, and relaunch behavior need simulator/device proof. |
| S04 | Trial, license, and purchase management | [`LicenseManagementView`](../VoiceInk/Views/LicenseManagementView.swift) | `partial`: shared tier definitions now record a Freemium allowance of 4,760 transcribed words/week, Italy reference pricing of $8/month with unlimited app usage, Roma reference pricing of $15/month with Italy plus best cloud inference and the Roma harness, and weekly-allowance arithmetic/block decisions; usage aggregation/reset, entitlement storage, quota enforcement, and purchase behavior are not wired into either app yet | shared/iOS | Decide account aggregation/reset and StoreKit/shared-entitlement transport, then wire enforcement and runtime-prove free-limit exhaustion/reset, purchase, restore, upgrade/downgrade, offline entitlement, and cross-platform access. |
| S05 | In-app update checking | Sparkle integration | App Store update distribution is the iOS equivalent | not-applicable | Sparkle must not be ported to iOS. |
| S06 | Menu bar, Dock visibility, launch at login, quit | [`MenuBarView`](../VoiceInk/Views/MenuBarView.swift), macOS settings | No iOS equivalent | macOS-only | These are desktop process/window lifecycle controls. |
| S07 | Reset app data/settings | macOS reset controls | `implemented, partially runtime-proven`: shipping iOS Settings exposes the shared reset plan with destructive confirmation; it removes records, files, preferences, and credentials, and clears onboarding progress | shared/iOS | [Exact-artifact stage `29534751513`](https://github.com/happyf-weallareeuropean/roma-just-talk/actions/runs/29534751513) proved the shipping reset control, destructive confirmation copy, confirmed reset, and relaunch into fresh Welcome. Confirmation cancellation and partial-failure behavior still need runtime proof. Evidence artifact: `8390706867`. |
| S08 | Roma Just Talk identity | [`VoiceInkAppIdentity`](../VoiceInkCore/Sources/VoiceInkCore/AppIdentity.swift), macOS bundle | `implemented, runtime proof pending`: iOS product/display names, navigation titles, shared copy, logging identity, deep links, and keyboard extension live in the single repo under the Roma Just Talk identity | shared/iOS | Config gates and exact-HEAD builds are required; installed app icon/name, keyboard label, onboarding, Settings, deep links, logs, and system permission surfaces still need simulator/device visual proof. |

## Fork Delta Ledger

The fork diverged from the audited upstream merge base at `0df2a9ab`. These
user-visible additions are not allowed to disappear from the parity audit.

| Fork addition | Commit evidence | Current iOS disposition |
|---|---|---|
| Voice recording pre-roll and buffer-first defaults | `8285be3d` | Implemented, device runtime proof pending; R09 tracks the ordinary fixed three-second recorder audio pre-roll. Speculative STT Preload is removed and intentionally excluded. |
| Special Shift recording mode and its subsettings | `a759c710`, `f19564b5` | Exact global-key mechanics are macOS-only (R16-R17); the iOS keyboard still needs complete dictation outcome parity (R02-R06). |
| Live-transcription auto policy | `3e8d9556` | Missing; R10 and R11 are intended on iOS. |
| Preload feature (speculative rolling VAD/STT and quick-release path) | `3000a89a` plus the v1.93-v1.96 latency/fallback fixes | Removed from macOS; do not port. R09 tracks only ordinary recorder audio pre-roll. |
| Cursor-context capitalization | `6dcff513` | Shared formatting exists, but iOS cannot use originating-field context until R03 and M15 are implemented. |
| Guided, refreshable permission setup | `7e6f0e28` plus v1.80/v1.96 fixes | Implemented with runtime proof for the app-owned microphone and keyboard setup surfaces across R04, R05, S01, and S02; real microphone-toggle return and successful recording retry still need device proof. |
| Roma Just Talk identity, fresh defaults, and hidden menu-bar default | `ec2bd29f`, `99666799` | Identity is source-present (S08); desktop menu/default behavior is macOS-only (S06); Parakeet default depends on missing FluidAudio (M04). |
| Quiet-local-audio level repair | v1.96 changelog and shared PCM policy | Implemented through shared core for macOS/iOS local Whisper; runtime audio proof remains required. |
| Failed-paste clipboard preservation and measured quick-release latency behavior | v1.96 changelog | Exact macOS paste behavior is platform-specific; iOS must prove one-time result delivery and insertion through R03/R06. |

## Confirmed Work Queue

Order follows user outcome dependencies, not implementation convenience.

1. Finish successful onboarding tutorial recording plus denied → Settings grant → same-process recording retry (R04, S01-S02; resume, skip, reset, dedicated permission UI, and R05 keyboard setup are runtime-proven).
2. Finish live third-party provider compatibility and keyboard result delivery (R06-R07; local and deterministic OpenAI-compatible remote/enhanced background completion, expiration, and cancellation are runtime-proven).
3. Finish manual-import proof: picker/Files share intake, long and multiple-file queues, live-provider transcription/enhancement, retry, and share-destination handoff (R08; external-open local and deterministic remote/enhanced completion, cancellation, relaunch persistence, and Share-sheet payload/presentation are runtime-proven).
4. Runtime-prove active streaming/live preview and continue VAD/prewarm controls (R10-R12).
5. Portable model/provider gaps (M03-M06, M08, M11-M12).
6. Prompt library and keyboard context (M14-M15).
7. History/export, metrics, retention, backup, and diagnostics (H03-H08).
8. Support/announcements and App Intents (S03, R20).

The goal cannot be marked complete while any `missing`, `partial`, `decision`,
or runtime-unverified `shared/iOS` row remains.
