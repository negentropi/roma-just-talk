# iOS Single-Repo Migration

`VoiceInk/` is the source repo. The sibling `../VoiceInk-iOS` checkout is evidence only.

## Migration Goal

Keep the macOS Roma Just Talk app as the behavior source of truth while making the iOS app live inside this repo as another platform shell. Shared transcription, prompt, provider, model, settings, history, and recording policy belongs in `VoiceInkCore/` so one core edit can be verified against both macOS and iOS. Platform-specific work stays in the macOS app, `iOS/VoiceInk-ios/`, `iOS/VoiceInkKeyboard/`, or `iOS/Shared/` only when it touches OS UI, permissions, audio/session execution, storage adapters, Keychain/UserDefaults execution, SwiftData execution, notifications, or app-extension communication.

Do not resurrect standalone-clone iOS services such as duplicate provider clients, hardcoded provider defaults, clone-local mode models, clone-local Keychain wrappers, clone-local WAV/VAD helpers, or clone-local prompt/provider enums. If a rule can be expressed without importing SwiftUI, AppKit, UIKit, AVFoundation, SwiftData, or app-extension APIs, first try to make it a `VoiceInkCore` policy or presentation type and keep the shell as an adapter. Before deleting duplicate iOS code, prove the replacement through the shared core checks and the iOS single-repo verifier; before changing shared behavior, prove macOS still keeps its current behavior.

## Current Shape

- macOS app: `VoiceInk.xcodeproj`
- iOS app and keyboard targets: `iOS/VoiceInk-ios.xcodeproj`
- iOS app target Swift sources: `iOS/VoiceInk-ios/*.swift`
- iOS app/keyboard shared shell code: `iOS/Shared/`
- iOS unit/UI test target sources: `iOS/VoiceInk-iosTests/`, `iOS/VoiceInk-iosUITests/`
- iOS non-Swift app artifacts: `iOS/VoiceInk-ios/PrivacyInfo.xcprivacy`, `iOS/VoiceInk-ios/Assets.xcassets/`, and `iOS/VoiceInk-ios/Resources/ggml-silero-v5.1.2.bin`
- iOS privacy policy source: `docs/ios-privacy-policy.md`
- shared Swift package: `VoiceInkCore/`
- workspace entry for both app projects: `VoiceInk.xcworkspace`
- standalone clone setup/Pages artifacts such as `iOS/.github/workflows/deploy.yml`, `iOS/.nojekyll`, `iOS/tasks.md`, `iOS/index.html`, `iOS/PRIVACY.html`, `iOS/PRIVACY.md`, and `iOS/app-icon.png` are intentionally absent; repo automation belongs at `VoiceInk/.github/` if it is needed later.

Both app projects reference the in-repo package:

- macOS package path: `VoiceInkCore`
- iOS package path: `../VoiceInkCore` from `iOS/`

Both app projects reference the same local Whisper framework location:

- `$(HOME)/VoiceInk-Dependencies/whisper.cpp/build-apple/whisper.xcframework`
- obsolete clone-side paths such as `../Downloads/build-apple/whisper.xcframework`, `../whisper.cpp/build-apple/whisper.xcframework`, and `../build-apple/whisper.xcframework` stay absent from project files.

No shared code should be added at the parent `faster-wisperflow/` workspace level.

## Shared Core

`VoiceInkCore` currently owns these cross-platform modules:

- app identity for shared display names, compact macOS title, sidebar subtitle, stable bundle identifier, CloudKit container identifier, iOS App Group identifier, iOS record deep-link contract, iOS recording Darwin notification names, the iOS app-local keyboard stop notification name, bundle-scoped error domains, support contact email address/subject/common-issues link/email-body/mailto policy, macOS AppIntent recorder title/description/dialog copy, macOS app diagnostics, iOS shell diagnostics, diagnostic log session storage/range/header/filename/error policy, diagnostic settings copy/icons, queue/window identifiers, local Whisper logging subsystem, macOS Application Support subdirectory, macOS navigation/file-transcription notification names and payload keys, macOS main-view sidebar labels/icons/visibility and notification-to-view selection mapping, iOS onboarding titles, and macOS storage alert copy/buttons; platform shells still own support system-info gathering, clipboard writes, mail composer execution, and OS URL opening
- app notification severity cases, SF Symbol names, default display duration, and failure-sound policy, colocated with app identity and event request contracts; platform shells still own SwiftUI color, panel/window execution, animation, timers, and sound playback
- iOS app/keyboard App Group recording state keys, stale-recording timeout, read-state decision, stale-state repair mutation planning, write plans, mutation-to-Darwin-notification plans, and App Group recording-state diagnostic copy; the iOS shared shell still owns the concrete App Group defaults suite and Darwin notification posting/observing
- iOS app/keyboard recording timing for app-launch recording start, keyboard recording-state polling, fallback button reset, deferred app-launch recording requests while onboarding is incomplete, and iOS recording coordination diagnostic copy; app and keyboard shells still own scheduling execution, notification delivery, and App Group mutation execution
- iOS keyboard recording button title/icon presentation for idle, recording, fallback, current-state selection, tap-action planning for stop-vs-open-app, keyboard open-main-app fallback order/diagnostic copy, and app-side keyboard stop-request handling; keyboard/app shells still own UIKit/SwiftUI rendering, colors, haptics, URL-opening execution, notification delivery, and App Group stop execution
- prompt templates and prompt text
- predefined prompt IDs, labels, prompt text, icons, descriptions, and system-instruction flags colocated with custom prompt persistence/repair policy
- custom prompt system-instruction wrapping
- transcription prompt preference loading/saving, selected-language local Whisper prompt fallback, and provider/use-specific request prompts for recorded-file, streaming, local, and direct transcription
- post-processing request construction and output filtering
- post-processing/enhancement failure presentation text and enhancement notification title truncation
- AI-enhancement error vocabulary, user-facing descriptions, HTTP status classification, Foundation transport-network classification, retry/backoff decision policy, non-enhancement transport-error retry planning, retry-progress/failure diagnostic messages, request payload/output filtering policy, and request rate-limit delay policy; platform shells still own provider transport, provider-specific error mapping, sleep timing, and logging
- provider catalog, provider endpoints, API key account names, provider readiness/selectability policy, and transcription empty-output policy
- provider API-key fallback environment-variable names
- provider credential nonblank validation for runtime API-key checks
- provider API-key draft verification candidate, verification start planning, save-after-verify policy, verification result application plan, form editing state, form presentation copy/action icons, verification progress vocabulary, and verification feedback presentation/icons for provider-key entry; macOS AI-enhancement provider-key entry shares draft enablement, form verification lifecycle, failure alert copy, and environment-reference resolution while preserving shell key storage
- provider API-key verification dispatch, including stored-key reference/fallback resolution before verification, blank-key rejection, HTTP response result mapping, non-HTTP failure copy, and transport-error result mapping
- provider API-key verification flag storage
- provider API-key state policy for stored-key lookup, runtime-key resolution, list rows, stored-key mutation plans, verification-flag mutation plans, verification reset on key changes, readiness calculation, and iOS storage failure diagnostic copy; platform shells still own Keychain/UserDefaults storage execution and UI editing state
- API-key display obfuscation policy; platform shells still own where stored keys are read and rendered
- Keychain generic-password query, data-store policy, and string-value storage policy for the shared app service, account key, syncability, data-protection keychain, UTF-8 values, add, copy-data, delete, delete-success status, and exists operations; platform shells still own macOS local-build fallbacks, logging, and UI state
- API-key environment-reference resolution and typed provider runtime-key lookup/fallback policy
- AI reasoning temperature, effort, provider-specific hidden-reasoning request parameters, and chat request-parameter planning for OpenAI-compatible post-processing/enhancement
- AI-enhancement provider identity, persisted-name parsing, selected-provider mutation planning, API-key requirement, text-enhancement selectability, connected-provider selection policy, credential-state policy, storage keys, model-selection key naming, and mapping to shared model providers; platform shells still own storage and execution
- AI-enhancement API-key verification route metadata, verification-request planning, and clear-key planning; native provider checks use shared verifier clients while macOS keeps model-specific Anthropic/OpenRouter/OpenAI-compatible verification
- AI-enhancement model defaults, available-model source selection, model-catalog source classification, settings-surface classification, static text-enhancement model lists, dynamic-provider default text-enhancement models, legacy Ollama service selected-model fallback, Ollama text-enhancement request temperature, selected-model fallback policy, selected-model mutation planning, and refresh-time dynamic-provider model-name/selection persistence application for macOS AI providers; platform shells still own dynamic providers such as Ollama, OpenRouter fetching, Local CLI, and Custom
- AI-enhancement provider request endpoint selection/parsing, invalid endpoint copy, execution route policy, OpenAI-compatible request execution planning, and optional API-key console URLs for macOS AI providers, including dynamic-provider request URL preferences; platform shells still own provider UI and request execution
- dynamic AI-provider preference storage for Ollama base URL/model, Custom provider base URL/model, and cached OpenRouter models; platform shells still own provider execution, network refresh, and UI state
- Local CLI template identity, command-template catalog, command/selected-template/timeout preference storage, timeout default/options/clamp, configured-command predicate, full-prompt wrapper, output trimming, command-failure classification, and execution error copy; macOS still owns PATH discovery, zsh execution, process timeout/termination, pipe IO, and environment injection
- transcription and AI model catalogs
- transcription provider recorded-file capability, including Cartesia as streaming-only
- Native Apple and FluidAudio local transcription model metadata, FluidAudio default macOS model selection, FluidAudio runtime version and language-hint policy, plus Native Apple unsupported-OS availability/error presentation, shared throwable runtime failure enum/copy, and result-stream timeout policy; platform shells still own availability checks, SDK version adaptation, download, and runtime adapters
- remote transcription provider dispatch for iOS retry transcription
- mode runtime configuration, default local mode selection, mode-list add/update/default repair, provider-change and stale model repair, mode-form provider availability, combined selected-mode/selected-language repair, mode-based transcription language availability, mode selection presentation for picker-vs-single-label UI, and provider model selection presentation
- mode provider-selection repair and draft saveability rules
- shared UserDefaults key names, including cleanup preferences, plus iOS mode persistence helpers with stale model repair on load
- shared preference default state, startup legacy-preference migration sets, app-settings reset state, and UserDefaults registration values for core-owned keys, including platform-specific selected-language defaults; platform shells still choose current model and register OS-specific keys
- array-backed preference list deletion policy for platform settings surfaces, colocated with shared UserDefaults preference policy
- macOS announcements enablement key/default, feed URL, refresh/initial-delay/request-timeout constants, dismissed-id storage/trimming, active-window filtering, next-announcement presentation, action copy, close icon, and description visibility; macOS still owns `Timer`, `URLSession`, AppKit panels/links, and SwiftUI rendering
- onboarding completion, iOS onboarding step order/copy/step presentation, macOS onboarding welcome presentation/typewriter roles, macOS onboarding model-download presentation/button/skip policy, macOS onboarding tutorial presentation, macOS onboarding reset-settings alert presentation, macOS onboarding permission step/audio-device/skip presentation/button policy, macOS permission-settings presentation, iOS settings chrome/action presentation, macOS top-level settings chrome/action presentation, iOS audio-session recording configuration plus lifecycle state transitions/diagnostic copy colocated in the shared iOS audio configuration surface, timeout storage/runtime policy/settings presentation, core-owned startup preference migration, user preference reset storage, and iOS app-data reset sequencing/file cleanup/diagnostic copy; platform shells still own first-run flow, onboarding layout/animation/actions, AVAudioSession activation/deactivation execution, permission checks/timers, permission settings UI state/actions, model download execution, SwiftData note deletion execution, keychain clearing, and settings UI bindings
- current transcription model preference loading/saving/clearing, load restore/clear decisions, plus legacy `"CurrentModel"` cleanup; platform shells still own concrete model lookup, model availability facts, and download/runtime state
- iOS local Whisper onboarding/model-management row action visibility, onboarding primary-action planning, deleteable downloaded-model visibility, downloaded-model delete request planning, and local-model management diagnostic copy; the iOS shell still owns button rendering, swipe rendering, download execution, file deletion, OSLog delivery, and UI refresh execution
- transcript status, default transcript fallback copy, status-panel and row-badge presentation, status symbol/tone metadata, canceled-transcript paste eligibility, presentation helpers, and localized standard transcript search semantics
- history and notes presentation for iOS notes, macOS history list, macOS no-selection, macOS no-metadata, macOS inline history search/no-search states, macOS history shortcut-tip copy/icon, macOS history search/pagination/selection controls, macOS history pagination state, load-more gating, selected-visible/all selection, selection toggling, select-all merge policy, selected-delete target planning, post-delete focus/selection repair, and search/latest-record refresh decisions; platform shells still own layout, colors, shortcut recording/rendering, SwiftData fetch execution, audio-file deletion, SwiftData deletion execution, and reload task execution
- failed/canceled transcript state planning, including macOS stored failure text, canceled-record metadata clearing, and iOS failure error detail
- error-description presentation fallback for shared record and shell failure text; `VoiceInkErrorDescription` lives with shared engine error vocabulary instead of a standalone shallow module
- local transcription/model/missing-audio error vocabulary, local Whisper failure mapping, and local Whisper optional language/prompt/VAD CString lifetime planning shared by macOS local Whisper, macOS retry/retranscribe surfaces, and iOS local retry transcription
- local Whisper transcription flow for context use, WAV sample loading failure mapping, transcription failure mapping, owned-context release policy, and platform transcription lifecycle diagnostic copy; platform shells still own model/context lookup, whisper.cpp adapters, OSLog delivery, and borrowed-context lifetime
- FluidAudio local ASR transcription policy for stable partial-transcript confirmation, streaming pass scheduling, seek math, cached-final hypothesis freshness, trailing-silence padding, and model-download status presentation; platform shells still own provider token adapters, ASR runtime calls, audio buffering, download execution, and event delivery
- streaming final-commit timeout policy for cloud providers versus local FluidAudio finalization, colocated with shared streaming route planning; platform shells still map runtime provider identity to the shared timeout source
- streaming transcription event vocabulary for session, partial, committed, and error events plus streaming error vocabulary/user-facing descriptions, colocated with shared streaming route planning; platform shells still adapt provider-specific event streams, and macOS still maps `LLMkit` transport errors into that taxonomy
- per-model transcription streaming preference key/default/read/write policy, streaming eligibility for unsupported/streaming-only/batch-capable models, provider-specific streaming connection model-name mapping, streaming/preload model-card presentation labels/help for cloud and local FluidAudio cards, and `VoiceInkTranscriptionSessionExecutionPlan` file/streaming session packaging; platform shells still own provider runtime adapters, concrete session instantiation, and settings UI layout
- streaming preference/model-name migration for legacy per-model streaming keys, removed realtime model selections, and stored Power Mode transcription model JSON; platform shells still own when startup migrations run
- rolling-buffer preload settings labels/help, per-model preload key/default policy, rolling VAD model identity/storage/import policy, configuration normalization, power-state model, rolling audio lead-in retention, service-route cloud/local classification, and preload allow/deny policy for global mode, per-model opt-out, cloud auto-disable, and low-battery local-model guards; macOS still owns IOKit power probing, rolling audio source delivery, VAD execution, runtime diagnostics, and model/provider adapters
- special-shortcut empty-tap fallback policy for press-duration threshold, pending fallback lifetime, completed-state checks, empty raw/enhanced transcript eligibility, and colocated key-evidence policy for typed-through/unreliable-key fail-closed decisions; macOS still owns pending fallback state, cursor paste, AppKit/CGEvent monitoring, and evidence collection
- recording-state vocabulary, macOS cancellation planning, microphone permission start/request/denied action planning, iOS permission settings-open fallback planning, rolling-preload preview eligibility, shortcut-action eligibility, recorder UI toggle mapping, recorder-session shortcut activity policy, recorder-style raw values/default/storage key/labels/window-kind policy, recording-start alert/notification presentation, iOS AVAudioRecorder configuration policy/start-false error shape, and recording transcription draft state for pending/canceled rows; platform shells still own recorder windows, timers, audio capture, OS permission status queries/requests, AVAudioRecorder dictionary adaptation, settings URL/open execution, notification dispatch, and OS-specific session state
- audio input mode raw values/default/presentation plus macOS audio-input settings presentation, storage keys/default registration, selected-device UID persistence, last-used microphone notification suppression, audio-device change/switch notification contract, priority device persistence shape, list add/remove/reindex/move policy, sorted priority order, first-available priority selection, and safe automatic input selection for preferred, built-in, safe non-Bluetooth fallback, and AirPods refusal; macOS still owns CoreAudio device discovery, transport/UID adaptation, selected-device side effects, persistence timing, and settings UI rendering
- raw transcription output filtering for hallucination tags/brackets, transcript whitespace normalization, optional filler words, default filler-word vocabulary, filler-word draft enablement, draft submission/reset/duplicate-alert planning, editor/list visibility, and filler-word list editing policy; iOS settings now use the same list editing and row presentation policy in their platform shell
- transcription cleanup preference loading/saving/reset storage, editable cleanup settings snapshots, settings presentation copy, macOS cleanup settings presentation/actions, raw-output filtering, filtered-text preparation, and paragraph-formatting/punctuation/lowercase/filler-word cleanup policy, with transcript text normalization colocated with the output filter
- transcription run text preparation for filtered text, word-replacement input, cleaned storage text, and short post-processing skip decisions, plus iOS transcription-run settings snapshots that bind mode, cleanup, language, prompt, word replacements, vocabulary, and skip preferences before execution; platform shells still own provider execution, prompt-trigger state mutation, AI-enhancement execution, persistence, and UI notifications
- filler-word list storage defaults and saving
- cursor-aware transcript capitalization policy; platform shells only supply cursor text and paste targets
- NaturalLanguage word-count policy for metrics and short-enhancement skip decisions
- session-metric calculation policy for enhanced-text word counting, non-positive duration cleanup, and transcription speed factor; platform shells still own SwiftData metric insertion and migration scheduling
- dashboard metrics summary, note-list summary/chrome/action presentation, derived productivity math for time saved, words per minute, keystrokes saved, average-WPM display text, macOS dashboard stat-card presentation, and macOS dashboard promotion visibility/copy/action URLs/dismissal key consuming the shared license state; platform shells still own metric queries, caching, navigation, URL opening, and rendering
- performance-analysis aggregation for transcription/enhancement model averages, speed factor, speed-factor/realtime display text, realtime comparison labels, average-processing-time display text, model-performance time-filter storage/date-window policy, and performance-panel presentation copy/icons; platform shells still own queries, system information, and rendering
- short post-processing skip policy, stored skip configuration, storage defaults, and macOS enhancement settings presentation/options for brief transcripts; platform shells still own UI controls and whether to apply the policy
- AI-enhancement timeout/retry storage keys, defaults, and runtime preference reads; platform shells still own request execution, provider transport, logging, and UI controls
- AI-enhancement enabled-state storage/diagnostics, selected-provider diagnostics, and per-provider selected-model preference storage, including legacy selected-provider repair; platform shells still own provider execution and dynamic model discovery
- transcription auto-cleanup enabled/retention preference storage, cutoff-date policy, and macOS settings presentation; platform shells still own SwiftData deletion and settings UI bindings
- audio-file auto-cleanup enabled/retention preference storage, cutoff-date policy, and macOS settings presentation/action copy; platform shells still own SwiftData queries, file deletion, timers, and settings UI state
- NaturalLanguage transcript paragraph formatting policy plus text-formatting storage key/default; platform shells still own the setting UI and when formatting runs
- word-replacement ordering, text application policy, and iOS UserDefaults persistence; macOS still owns SwiftData dictionary storage
- vocabulary draft text/submit/reset/duplicate-alert planning, word-replacement draft text/visibility/submit/reset/duplicate-alert planning, word-replacement edit state/saveability/update-completion planning, list application for accepted submissions, backup dictionary insert/export planning, vocabulary backup record shape, word-replacement backup import/export planning, word-replacement edit planning, dictionary alert titles, dictionary persistence failure copy, dictionary settings form/chrome presentation copy, macOS dictionary quick-add presentation copy and draft-state use, dictionary list/row presentation copy, dictionary list sort mode storage/toggle/indicator policy, and vocabulary/word-replacement list sorting; platform shells still own macOS SwiftData dictionary storage and persistence execution
- custom vocabulary term normalization plus provider/use-specific vocabulary support and limits for transcription providers and iOS UserDefaults persistence; macOS still owns SwiftData vocabulary storage
- custom cloud transcription model draft normalization, generated-name, required-field, validation policy, macOS form defaults, form presentation copy/icons, default model description, keychain failure copy, custom OpenAI-compatible batch request defaults, API error-domain classification, invalid-endpoint copy, and empty-result acceptance policy; platform shells still own keychain, preferences storage, endpoint execution, and error presentation
- prompt trigger-word detection, trigger-word draft validation, trigger-word add/remove editing policy, prompt-trigger AI-enhancement detection result construction, and prompt-trigger settings-state application/restoration planning; platform shells still own prompt persistence and enhancement state mutation execution
- AI-enhancement prompt context assembly for selected text, clipboard, current-window text, custom vocabulary prompt text, custom vocabulary tags, and screen-capture context window selection/text formatting; platform shells still own OS access, capture, and vocabulary storage
- AI-enhancement result shape and completed-result construction for enhanced text, duration, selected model, prompt name, and request message metadata; transcription run results carry successful post-processing metadata through the same shape, derive success and enhancement duration from that result, and preserve attempted-model/error fields; platform shells still own provider execution and persistence targets
- AI-enhancement custom prompt record shape, Codable compatibility, and final-prompt wrapping; macOS still owns SwiftUI prompt rendering and prompt-store orchestration
- predefined-prompt metadata, repair/merge policy, startup prompt-store repair ordering, enable-time selected-prompt fallback planning, and trigger-detectable prompt filtering; macOS still owns when the prompt store is loaded and saved
- AI-enhancement custom prompt array mutation, backup import/export prompt filtering/merge policy, selected-prompt repair, and selected-prompt persistence helpers; platform shells still own notifications and UI state updates
- selected-prompt repair, enable-time prompt settings-state fallback, and base prompt-text selection for AI enhancement; platform shells still own context capture and request execution
- AI-enhancement prompt-store storage keys and context-toggle storage; macOS still owns persistence timing, notifications, and UI orchestration
- transcription language catalog, provider language filtering, AssemblyAI realtime/batch language policy, selected-language fallback policy, language-option ordering, selected-language preference loading/saving/clearing, compatible selected-language persistence planning, selected-language request normalization, language-selection presentation copy, and the shared selected-language defaults key; platform shells still own selected-language UI, persistence execution, notifications, and runtime streaming-mode state
- transcription language-source policy for Whisper, Native Apple, FluidAudio, shared provider catalogs, AssemblyAI realtime/batch language selection, all-language fallback, and Native Apple language asset-control presentation; platform shells still adapt their model/provider enums and live streaming preferences
- local Whisper language seed prompts and custom language-prompt storage; platform shells still own prompt editing UI and when prompts are persisted
- stored audio-file path resolution, existing-file lookup, availability/missing-audio presentation, unavailable icon, deletion error text, recordings directory, file URL construction, recording/import/retranscription filename policy, and record-level resolved/existing/delete/delete-and-clear helpers; iOS audio-player UI consumes availability-gated paths instead of probing files itself
- supported media extension and UTType import policy for audio/video files
- transcription CSV header, row formatting, and CSV escaping; platform shells still own save panels and record-source mapping
- power-mode display formatting for transcript metadata and exports
- power-mode stored config Codable shape, parsed prompt/provider application state, preference-application plans, session application plans, selected prompt-title resolution, add/edit form state, AI-enhancement selection default repair, provider/model picker selection repair, transcription model resource plans, selected-language application plans, begin-session and snapshot-update lifecycle plans, session diagnostic copy, legacy key decoding, UserDefaults config and active-config-id persistence, trigger-to-rule adaptation, config-list lookup/mutation/reordering, enabled-list selection, backup shortcut import key filtering, app/website/default matching, website URL normalization, website form-input config construction, browser metadata, browser URL and detection diagnostic copy, form trigger-list add/remove editing policy, configuration-name saveability, add/edit form mode, form-draft to stored-config construction, save validation, auto-send key stored value/display/enablement state, active-session snapshot Codable shape, active-session snapshot construction from prompt IDs and cleanup settings, restore-ready prompt IDs/provider IDs and cleanup state, cleanup legacy decoding, and active-session UserDefaults persistence; macOS still owns OS app/website detection, SwiftUI editing/alerts, active-session application/restoration, shortcuts, auto-send key execution, and config/session persistence timing
- duration presentation, including positive-duration display visibility and metadata separator text
- relative timestamp presentation plus macOS history/detail timestamp presentation, colocated with duration presentation in the temporal presentation policy
- Whisper and VAD model file metadata, including platform-base Whisper model directory creation, downloaded local-model file records, runtime model-name to downloaded-file resolution, model/sidecar file construction, downloaded-state detection, bootstrap-model availability, Core ML support policy, final `.bin` install/replacement, importable local-model extension policy, model/sidecar deletion, accepted HTTP download status policy, iOS simple-download install/failure/cancellation planning, local-model operation confirmation presentation, and local-model operation alert presentation
- local Whisper runtime defaults and runtime configuration for request-language normalization, prompt, whisper.cpp runtime flags, thread count, transcription temperature, VAD enablement, VAD model path eligibility and warning copy, VAD thresholds, local Whisper failure mapping, context initialization GPU/flash-attention policy plus context/full/VAD parameter assignment, and raw segment lookup/concatenation
- VAD bundle resource lookup
- PCM16 sample conversion, mono 16 kHz transcription-audio format constants, WAV file sample loading, mono PCM16 duration calculation, normalized mono sample mixing for Whisper input, float-to-PCM16 output sample conversion, interleaved Float32 live-capture conversion to mono PCM16 with optional linear resampling, and sample-aligned PCM16 data chunking, including bit depth, endian, and integer/float sample policy
- audio-meter decibel normalization, smoothing math, macOS/iOS meter update plans, platform update cadences, bounded visualizer history, iOS visualizer geometry/width/height/recent-sample selection, and visualizer accessibility label; platform shells still own capture, timer implementation, smoothing state storage, layout containers, colors, and visualizer rendering
- audio playback timeline progress, waveform sample progress, playback update cadence, completion tick planning, timer side-effect hints, playback tick action application, playback state transitions, clamped seek-time math, playback-rate persistence/cycling, playback-rate button presentation, iOS playback session category policy, loading/play-pause/metadata-icon presentation, iOS playback failure diagnostic copy, and macOS audio-player action help copy; platform shells still own `AVAudioPlayer`, audio-session execution/adaptation, timer/seek execution, OSLog delivery, gestures, hover state, and playback UI
- OpenAI-compatible, Deepgram, Gemini, Mistral, ElevenLabs, xAI, Soniox, Speechmatics, and AssemblyAI remote transcription request/client helpers
- shared remote transcription provider dispatch plus batch request option defaults for provider/use-specific prompt, vocabulary, timeout, retry, and formatting parameters
- shared API-key verification result policy plus Cartesia API-key verification request/client helper colocated with provider API-key verification
- shared multipart form-data construction for remote transcription clients
- shared remote HTTP response validation, provider-domain error construction, retryable status-code classification, direct and retried validated request helpers, and long-running polling loop policy for remote transcription clients plus OpenAI-compatible chat completion

Current macOS consumers of shared remote transport:

- macOS batch cloud transcription uses `CloudProvider` default dispatch into `VoiceInkRemoteTranscriptionService` for Groq, Deepgram, Gemini, Mistral, ElevenLabs, xAI, Soniox, Speechmatics, and AssemblyAI; `CloudProviderRegistry` now maps shared provider identity to macOS streaming factories, cloud transcription failure copy comes from `VoiceInkCloudTranscriptionError`, and empty-response policy comes from `VoiceInkProviderKind`.
- `VoiceInkRemoteTranscriptionService`, `VoiceInkRemoteTranscriptionOptions.batchDefaults`, and the colocated `VoiceInkCloudTranscriptionError` preserve macOS provider-specific batch options and failure descriptions such as Groq JSON/temperature/retry settings, Deepgram paragraph/timeout settings, prompt/vocabulary forwarding, unsupported-provider errors, missing-key errors, HTTP API status/domain mapping, network errors, and empty-response errors; the remote transport module colocates `VoiceInkRemoteHTTPResponsePolicy`, `VoiceInkRetriedRequest`, and `VoiceInkRemotePollingPolicy` for shared HTTP validation, retry, and polling helpers.
- Custom OpenAI-compatible batch transcription uses `VoiceInkOpenAICompatibleTranscriptionClient` with request defaults, API error domain, invalid-endpoint copy, no-plain-text-fallback decoding, and empty-result acceptance from `VoiceInkCustomCloudTranscriptionPolicy`; HTTP API status/domain mapping comes from `VoiceInkCloudTranscriptionError`, and empty-response error copy comes from `VoiceInkTranscriptionRunError`.
- Cartesia API-key verification uses `VoiceInkProviderAPIKeyVerifier`, with its request/client helper colocated there, and `VoiceInkAPIKeyVerificationPolicy` through `VoiceInkTranscriptionModelProvider`; Cartesia transcription remains streaming-only in platform shell code.
- MacOS cloud-provider API-key verification uses `VoiceInkProviderAPIKeyDraft`, `VoiceInkProviderAPIKeyFormState`, `VoiceInkProviderAPIKeyVerificationProgress`, common verification-result handling from `VoiceInkAPIKeyVerificationPolicy`, and stored-key verification through `VoiceInkProviderAPIKeyVerifier`, including transcription-model providers such as Cartesia; provider-specific streaming adapters still own transcription execution.
- macOS local Whisper/model loading throws `VoiceInkEngineError` from `VoiceInkCore`; macOS error descriptions are covered by `VoiceInkEngineErrorTests`.
- macOS local Whisper and cloud transcription normalize selected request language through `VoiceInkTranscriptionLanguagePreference`.
- macOS local Whisper, cloud transcription, AssemblyAI streaming, and `WhisperPrompt` read/write transcription prompts through `VoiceInkTranscriptionPromptPreference`; local Whisper transcription uses the shared selected-language prompt fallback helper, and macOS/iOS Whisper C adapters use `VoiceInkWhisperRuntimeInvocationPlan` for language/prompt/VAD model path pointer lifetime plus `VoiceInkWhisperRuntimeFullParameterSink`/`VoiceInkWhisperRuntimeVADParameterSink` for full/VAD parameter assignment while keeping concrete whisper.cpp execution in the platform shells.
- macOS live recording, audio-file import, and retry transcription route transcript prep through `VoiceInkTranscriptionEnhancementTextPlan` for raw filtering, word replacement, cleaned storage text, enhancement text, filtered-text latency counts, short-enhancement skip decisions, and enhancement request planning, including prompt-trigger forcing and processed prompt text selection; audio-file call sites keep the narrower `VoiceInkAudioFileTranscriptionTextPlan` alias. These paths use `VoiceInkPowerModeTranscriptionMetadata` for enabled-only Power Mode name/emoji attachment while keeping macOS prompt-trigger state, AI request capture, active Power Mode lookup, and SwiftData persistence in the shell.
- macOS and iOS local Whisper read `VoiceInkWhisperRuntimeConfiguration` to assemble language, prompt, whisper.cpp runtime flags, thread count, temperature, VAD settings, and shared diagnostic labels, then apply those values through the shared parameter-sink Interface before calling whisper.cpp; raw whisper.cpp segment count/text lookup and concatenation use `VoiceInkWhisperTranscriptSegments`, colocated with local Whisper runtime policy. MacOS local Whisper and iOS local Whisper route platform request defaults, context-use/sample-load/transcription/release orchestration, and service lifecycle log text through `VoiceInkLocalWhisperTranscriptionRequest`, `VoiceInkLocalWhisperTranscriptionFlow`, and `VoiceInkLocalWhisperTranscriptionDiagnostics`, while platform shells still resolve model contexts, deliver OSLog messages, and macOS preserves borrowed preloaded-context lifetime plus thrown sample-read error behavior. FluidAudio VAD gating still reads `VoiceInkVADPreference`, preserving the existing `IsVADEnabled` storage key while sharing the default and lookup policy with iOS.
- macOS `WhisperModelManager` validates model-download HTTP responses and temporary-file completion state through `VoiceInkWhisperModelDownloadResponsePolicy`; the macOS shell still owns URLSession progress, Core ML sidecar download/unzip, notifications, and local model list state.
- macOS `WhisperPrompt` loads/saves custom local-Whisper language prompts through `VoiceInkLocalWhisperPromptCatalog`, and macOS local Whisper output-format settings labels/help/actions route through the same catalog; macOS and iOS local Whisper fallback prompts now read stored custom prompts through the shared catalog, with model managers kept out of prompt object ownership.
- macOS `AudioProcessor` delegates audio-processing error vocabulary, AVAudioFile channel averaging and normalization for Whisper samples, plus float-to-PCM16 WAV sample conversion to `VoiceInkAudioProcessingError` and `VoiceInkPCM16Audio`; macOS still owns AVAudioFile reading, sample-rate conversion, and WAV writing.
- macOS local Whisper, macOS FluidAudio, and iOS local Whisper load WAV-file PCM16 samples through `VoiceInkWhisperAudioSamples` and `VoiceInkPCM16Audio`; the Whisper-specific leveling-default adapter lives with PCM16 conversion instead of a standalone shallow module, while platform shells still own model/context lifetime and provider-specific error mapping.
- macOS and iOS local Whisper adapters map model-unavailable, model-load, audio-processing, and whisper-core transcription failures through `VoiceInkLocalWhisperFailurePolicy`; platform shells still own logging, context lifetime, and provider execution.
- macOS `CoreAudioRecorder` delegates interleaved Float32 device-sample mixing, optional linear resampling, and PCM16 clipping to `VoiceInkPCM16Audio`; macOS still owns CoreAudio buffers, pre-roll buffering, file writes, and live chunk delivery.
- macOS pre-roll streaming and buffered-snapshot streaming split mono PCM16 data through `VoiceInkPCM16Audio.monoPCM16Chunks`, derive quick-release durations through `VoiceInkPCM16Audio.duration(forMono16kData:)`, and size rolling-preload buffers through `VoiceInkPCM16Audio.byteCount(forMono16kDuration:)`; macOS still owns streaming sessions, callbacks, and quick-release orchestration.
- macOS batch cloud and streaming transcription use `VoiceInkProviderCredential` for runtime API-key presence checks before entering provider adapters.
- macOS API-key lookup reads provider fallback environment-variable names and custom-model Keychain account identifiers from `VoiceInkProviderAPIKeyAccount`, colocated with provider access requirements in the shared provider catalog; Keychain access remains in the macOS shell.
- macOS cloud-provider model lists are supplied by the `CloudProvider` default adapter over `VoiceInkTranscriptionModelCatalog`; the old standalone cloud-model adapter file is folded into `CloudProvider`, so the macOS shell only owns streaming factories and platform storage context.
- macOS cloud-provider and model recorded-file/streaming-only support is derived from `VoiceInkTranscriptionModelProvider`, so Cartesia remains streaming-only without a shell-only override.
- macOS cloud-provider HTTP status error-domain mapping, shared remote transcription transport error domains, low-level remote client defaults, direct and retried request execution plus validation, provider-domain NSError construction, retryable status-code classification, Soniox/Speechmatics/AssemblyAI polling cadence and timeout mechanics, and OpenAI-compatible chat completion response validation are derived through `VoiceInkTranscriptionModelProvider.apiErrorDomain` plus the colocated remote transport helpers `VoiceInkRetriedRequest`, `VoiceInkRemotePollingPolicy`, and `VoiceInkRemoteHTTPResponsePolicy`, so batch transport error classification shares provider metadata instead of shell-only switches or provider literals scattered through clients.
- macOS Native Apple and Parakeet model structs adapt `VoiceInkTranscriptionModelCatalog` local model specs; default macOS FluidAudio model selection, availability requirements, runtime version, and language-hint decisions come from `VoiceInkTranscriptionModelCatalog.defaultMacOSFluidAudioModelName`, `VoiceInkTranscriptionModelAvailabilityFacts`, `VoiceInkFluidAudioModelVersion`, and `VoiceInkTranscriptionModelCatalog.fluidAudioLanguageHintCode`, while macOS only maps them to concrete registry and FluidAudio SDK types. Native Apple selection/runtime failures throw `VoiceInkNativeAppleTranscriptionFailureKind`, runtime failure copy and result-stream timeout math are colocated with the model catalog through `VoiceInkNativeAppleTranscriptionPolicy`; macOS still owns OS availability checks and FluidAudio download/runtime code.
- macOS language pickers and iOS language settings use `VoiceInkLanguageCatalog.sortedOptions`, `VoiceInkLanguageCatalog.displayName`, and `VoiceInkTranscriptionLanguagePresentation` so language presentation order, fallback naming, and picker copy stay shared.
- macOS recording, audio-file transcription, and retry transcription use `VoiceInkTranscriptionCleanupConfiguration` directly for shared raw-output filtering and cleanup preferences.
- macOS model definitions read supported language sets through a thin `ModelProvider` adapter backed by `VoiceInkLanguageCatalog`; the old `LanguageDictionary` wrapper is gone.
- macOS metrics dashboard duration copy uses `VoiceInkDurationPresentation`, removing the last dashboard-local duration formatter.
- macOS predefined prompt persistence adapts `VoiceInkPredefinedPrompts` into `VoiceInkCustomPrompt`, both colocated with shared custom-prompt policy, so stable prompt IDs and metadata live in shared core.
- macOS custom prompt final text delegates to `VoiceInkAIPrompts.finalPromptText`, so the system-instruction wrapper stays shared.
- macOS uses `VoiceInkCustomPrompt` directly, keeping the SwiftUI prompt-grid adapter in the macOS shell while moving the prompt record and Codable compatibility into `VoiceInkCore`.
- macOS predefined-prompt initialization now delegates to `VoiceInkCustomPromptPolicy`, and the old shell-only `PredefinedPrompts` adapter was removed.
- macOS custom prompt draft saveability, add/update record construction, and add/update/delete selection behavior delegate to `VoiceInkCustomPromptPolicy`; macOS `AIEnhancementService` still owns persistence timing, notifications, and SwiftUI state.
- macOS custom prompt backup export filtering and import merge ordering delegate to `VoiceInkCustomPromptPolicy`; macOS still owns backup file encoding/decoding, panels, alerts, and service state mutation.
- macOS custom prompt icon catalog, prompt-card trigger summary/action symbols, grid empty/help copy and info icon, editor labels/placeholders/help/action symbols, and delete confirmation copy route through `VoiceInkCustomPromptPresentation`; macOS still owns SwiftUI layout, popovers, drag/drop, and alert execution.
- macOS custom prompt loading, saving, and selected-prompt persistence now use `VoiceInkCustomPromptStorage`.
- macOS custom prompt startup prompt-store repair ordering now delegates to `VoiceInkCustomPromptPolicy.startupStoreState`; macOS `AIEnhancementService` still owns persistence timing, notifications, and UI state.
- macOS AI-enhancement prompt selection delegates selected-prompt repair, enable-time prompt settings-state fallback, and base prompt text to `VoiceInkCustomPromptPolicy`; macOS still appends selected-text, clipboard, screen-capture, and vocabulary context before executing provider requests.
- macOS AI-enhancement provider-key validity changes delegate enhancement-disable state planning to `VoiceInkCustomPromptPolicy`; macOS still owns observable-object invalidation, preference writes, notifications, and provider-key storage.
- macOS PowerMode AI-enhancement prompt setup delegates nil-selection fallback to `VoiceInkCustomPromptPolicy`; PowerMode still owns OS app/website detection, AI provider/model UI, active-session application, shortcuts, and config persistence.
- macOS `PowerModeManager`, `ActiveWindowService`, `PowerModePopover`, and `PowerModeConfigView` call `VoiceInkPowerModePolicy`, `VoiceInkPowerModeConfigurationPreference`, `VoiceInkPowerModeBrowser`, `VoiceInkPowerModeBrowserURLDiagnostics`, `VoiceInkPowerModeBrowserDetectionDiagnostics`, and shared `PowerModeConfig` array helpers directly for config/active-id storage, URL normalization, app/website/default matching, browser metadata, browser URL and detection diagnostic copy, explicit/website/app/default resolution order, enabled-rule checks, config-list mutation/reordering, enabled-list display, and save validation; macOS uses `VoiceInkPowerModeValidationError` directly while keeping frontmost-app/browser URL detection, SwiftUI alerts, storage timing, notifications, shortcuts, and OS-trigger discovery in the shell.
- macOS Power Mode backup import/export plan delegates existing-config shortcut cleanup ids, imported/exported configuration payloads, valid imported-config shortcut key filtering, shortcut backup-key export, empty-shortcut export omission, custom emoji backup payloads, custom emoji backup-record presence, and config success counts to `VoiceInkPowerModePolicy.powerModeBackupImportPlan`/`powerModeBackupExportPlan`; macOS still owns `ShortcutStore` reads/writes, `PowerModeManager` mutation/save timing, `EmojiManager` mutation, and AppKit/Carbon shortcut decoding.
- macOS `PowerModeConfigView` initializes editable add/edit state through `VoiceInkPowerModeConfigurationFormState`, so latest-config refresh, add defaults, optional trigger-list normalization, and transcript-formatting expansion policy live in shared core while SwiftUI `@State` ownership stays in the shell.
- macOS Power Mode config form seed defaults, trigger-section labels, transcription section labels, AI toggle title, footer actions, website add planning, and trigger removal route through `VoiceInkPowerModeConfigurationFormState`/`VoiceInkPowerModePresentation`/`VoiceInkPowerModePolicy`; macOS still owns SwiftUI layout, AppKit app discovery/icons, state assignment, save/delete dispatch, and field focus.
- macOS Power Mode app picker search copy and app-trigger toggle policy route through `VoiceInkPowerModePresentation`/`VoiceInkPowerModeAppConfig`; macOS still owns NSWorkspace app discovery, app icons, popover layout, and search filtering.
- macOS Power Mode configuration arrays use shared `PowerModeConfig`, `VoiceInkPowerModeAppConfig`, and `VoiceInkPowerModeURLConfig`, so the stored config Codable shape, legacy key decoding, id equality, and policy-rule adaptation live in shared core while OS app picker discovery stays in the macOS shell.
- macOS `PowerModeSessionManager` stores and loads `VoiceInkPowerModeSession` plus `VoiceInkPowerModeApplicationState` through `VoiceInkPowerModeSessionPreference`, delegates begin-session and snapshot-update lifecycle decisions to `VoiceInkPowerModeSessionBeginPlan`/`VoiceInkPowerModeSessionSnapshotPlan`, builds config/restore side-effect order through `VoiceInkPowerModeSessionApplicationPlan`, applies config/restore preference plans through `VoiceInkPowerModePreferenceApplication`, delegates selected-model resource decisions to `VoiceInkPowerModeTranscriptionModelResourcePlan`, delegates local Whisper model lookup to `VoiceInkWhisperModelFiles`, delegates selected-language repair decisions to `VoiceInkPowerModeLanguageApplicationPlan`, and uses `VoiceInkPowerModeSessionDiagnostics` for console copy; provider/model resource loading, language persistence, observer registration, notifications, recovery timing, and logging execution stay in the macOS shell.
- macOS top-level Power Mode visibility, persist-after-recording preference, first-run visibility repair, and shortcut eligibility use `VoiceInkPowerModePreference`; macOS still owns the settings UI, active OS app/website detection, shortcut monitoring, and Power Mode session execution.
- shared `PowerModeConfig`, macOS `PowerModeConfigView`, Power Mode cards, and `CursorPaster` use `VoiceInkAutoSendKey` for the stored auto-send option, picker labels, and enablement check; macOS still owns `CGEvent` key posting after paste.
- macOS Power Mode panel add-button title and add/edit form title mode come from `VoiceInkPowerModeConfigurationMode`; macOS still owns panel presentation and animation.
- macOS Power Mode settings row, disable-alert copy, panel, sidebar empty state, reorder sheet, badges, row actions, manual-selection popover copy, panel help URL, and panel/reorder/sidebar/form/app-picker/popover/row trigger/action symbols route through `VoiceInkPowerModePresentation`; macOS still owns SwiftUI layout, settings bindings, panel/popover presentation, drag/reorder execution, and selection actions.
- macOS prompt-trigger detection delegates result construction and settings-state application/restoration planning to `VoiceInkPromptDetectionPolicy`/`VoiceInkPromptDetectionResult`; macOS `AIEnhancementService` only applies the returned shell state, and the old shell-only `PromptDetectionService` adapter was removed.
- macOS final paste text assembly delegates cursor-context capitalization planning, lowercase-cleanup preference gating, the trial-expired prefix, trailing-space storage/default registration, and trailing-space settings labels/help to `VoiceInkTranscriptionPasteOutputPolicy`/`VoiceInkAppendTrailingSpacePreference`; `CursorPaster` adapts the shared cursor plan to macOS cursor lookup, while macOS still owns cursor paste, clipboard context lookup, sound feedback, auto-send timing, and settings rendering.
- macOS paste method and clipboard-restore preferences use `VoiceInkPasteMethod`/`VoiceInkPastePreference` for raw storage keys, legacy AppleScript migration, display labels, defaults, bounded restore delay, settings presentation labels/options/help, and backup import/export plans; macOS still owns AppKit pasteboard access, AppleScript/CGEvent paste execution, and SwiftUI settings rendering.
- macOS recording feedback preferences use `VoiceInkSystemMuteMode`/`VoiceInkRecordingFeedbackPreference` for raw storage keys, legacy mute-boolean compatibility, experimental-feature backup import/export, pause-media fallback, display labels, defaults, recording/pause-media settings presentation labels/options/help, diagnostics, and backup import/export plans. macOS custom recording sounds route built-in sound catalog metadata, custom-sound storage keys/defaults, notification name, custom-sound URL and copy planning, duration limit, validation error copy, settings/menu/open-panel copy and icons, playback slots, volumes, and custom/default fallback order through `VoiceInkBuiltInRecordingSound`/`VoiceInkCustomSoundType`/`VoiceInkCustomSoundPreference`/`VoiceInkCustomSoundSettingsPresentation`/`VoiceInkRecordingSoundPlaybackPolicy`; macOS still owns CoreAudio mute execution, MediaRemote pause/resume execution, bundle sound lookup, custom sound file IO execution, AVFoundation validation/playback, file-picker execution, and SwiftUI settings rendering.
- macOS VAD, model prewarm, and recorder transcript-preview preferences use `VoiceInkVADPreference`/`VoiceInkModelRuntimePreference`/`VoiceInkRecorderPreviewPreference` plus `VoiceInkMacOSAdvancedTranscriptionSettingsPresentation` for raw storage keys, defaults, registration/reset behavior, and settings labels/help; macOS still owns wake/app-launch scheduling, model-manager checks, and SwiftUI settings/recorder rendering.
- macOS recording shortcut action identifiers, action display names, validation and monitor notification copy, mini-recorder escape confirmation copy, selection/mode migration plans, legacy shortcut key names, raw shortcut storage, middle-click, and special empty-tap preferences use `VoiceInkShortcutActionIdentifier`/`VoiceInkShortcutActionPresentation`/`VoiceInkShortcutValidationPresentation`/`VoiceInkMacOSShortcutNotificationPresentation`/`VoiceInkLegacyRecordingShortcutPreset`/`VoiceInkRecordingShortcutSelection`/`VoiceInkRecordingShortcutMode`/`VoiceInkRecordingShortcutPreference`/`VoiceInkShortcutStoragePreference`/`VoiceInkShortcutBackupPolicy` for raw values, shortcut `Data`/cleared-state storage, display labels, settings labels/help, shortcut recorder labels, current and legacy storage keys, defaults, diagnostics, reset behavior, backup import/export value planning, general-backup shortcut action ordering, and recording-shortcut selection repair when importing backed-up shortcut records; macOS still owns AppKit/Carbon shortcut decoding/materialization, validation checks/conflict lookup, event monitoring, permission handling/settings opening, notification delivery, dynamic Power Mode name lookup, `ShortcutBackup` records, and settings/recorder rendering.
- macOS AI-enhancement prompt-store preferences and context toggles now use `VoiceInkUserDefaultsKey`/shared preference helpers, preserving the same raw storage names while making future iOS prompt-store adoption share the same storage policy.
- macOS AI-enhancement provider selection uses `VoiceInkAIEnhancementProviderKind` directly and delegates selected-provider mutation planning to `VoiceInkAIEnhancementProviderSelectionPlan`, while `VoiceInkUserDefaultsKey` keeps persisted provider identity, selectable text-enhancement providers, model-selection keys, and API-key requirement shared; macOS keeps dynamic provider settings and execution adapters.
- macOS AI-enhancement stored-provider parsing delegates canonical and legacy values to `VoiceInkAIEnhancementProviderKind`; macOS still owns the actual `UserDefaults` and Power Mode config storage.
- macOS AI-enhancement connected-provider selection and dynamic-provider shell triggers delegate provider readiness, connected-provider key-storage name selection, selected-provider dynamic refresh planning, and provider classification policy to `VoiceInkAIEnhancementProviderKind`/`VoiceInkAIEnhancementProviderSelectionPlan`; macOS still supplies live API-key, Ollama, and Local CLI readiness plus the concrete refresh/sync operations.
- macOS AI-enhancement current-model selection delegates selected-model fallback, selected-model mutation planning, default text-enhancement model policy, available-model source selection, static model-list fallback, and refresh-time dynamic-provider model-name/selection application planning to `VoiceInkAIEnhancementProviderKind`/`VoiceInkAIEnhancementModelSelectionPlan`/`VoiceInkAIEnhancementModelRefreshPlan`; macOS still supplies live dynamic Ollama/OpenRouter model lists and applies Ollama runtime model changes.
- macOS AI-enhancement API-key drafts, form verification lifecycle, credential-state selection, credential-state resolution planning, Local CLI credential refresh, environment-reference resolution, failure-message copy, verification-request planning, clear-key planning, clear-key storage-name planning, verification-result application planning, verification-dispatch planning, verified-key persistence application, verified-key persistence storage-name planning, service/view verification-result flow, and verification dispatch read `VoiceInkAIEnhancementAPIKeyDraft`, `VoiceInkAIEnhancementCredentialStateResolutionPlan`, `VoiceInkAIEnhancementAPIKeyVerificationRequestPlan`, `VoiceInkAIEnhancementAPIKeyVerificationDispatchPlan`, `VoiceInkAIEnhancementAPIKeyClearPlan`, `VoiceInkAIEnhancementAPIKeyFormState`, `VoiceInkAPIKeyVerificationResult`, plus `VoiceInkAIEnhancementProviderKind`; native provider checks route through `VoiceInkProviderAPIKeyVerifier`, while Anthropic, OpenRouter, OpenAI-compatible, and Custom keep macOS LLMkit model-specific verification adapters.
- macOS AI-enhancement provider model defaults, model-catalog source classification, settings-surface classification, available-model source selection, static model lists, selected-model mutation planning, and refresh-time dynamic-provider model-name/selection application planning come from `VoiceInkAIModelCatalog`/`VoiceInkAIEnhancementProviderKind`/`VoiceInkAIEnhancementModelSelectionPlan`/`VoiceInkAIEnhancementModelRefreshPlan`; macOS still owns provider UI rendering, API-key storage, dynamic OpenRouter fetches, Ollama, Local CLI, and Custom provider settings.
- macOS AI settings reads the shared Ollama default text-enhancement model constant when initializing local dynamic-provider model state; macOS still owns SwiftUI state and Ollama settings controls.
- macOS AI-enhancement request endpoint parsing, invalid endpoint copy, execution route policy, OpenAI-compatible request execution planning, dynamic-provider request URL selection, and API-key console URLs come from the shared `VoiceInkAIModelProvider` and `VoiceInkAIEnhancementProviderKind` catalogs; macOS still owns Anthropic/OpenAI-compatible transport adapters, provider-specific verification adapters, and SwiftUI rendering.
- macOS AI-enhancement failures throw `VoiceInkAIEnhancementError`, classify HTTP status, generic LLMkit transport failures, retryable Foundation transport-network failures, and Ollama failure copy/retry shape plus generic transport-failure mapping through core, route retry decisions, non-enhancement transport-error retry planning, and retry-progress/failure diagnostic messages through `VoiceInkAIEnhancementRetryState`/`VoiceInkAIEnhancementNonEnhancementErrorRetryPlan`/`VoiceInkAIEnhancementRetryProgressPresentation`/`VoiceInkAIEnhancementRetryFailurePresentation`, prepare request preflight/user-message payloads and final enhanced text through `VoiceInkAIEnhancementRequestPreparation`/`VoiceInkAIEnhancementRequestPayload` plus the colocated `VoiceInkAIEnhancementOutputFilter` in the shared AI prompt module, and calculate request rate-limit delays through `VoiceInkAIEnhancementRateLimitPolicy` from `VoiceInkCore`; macOS still owns LLMkit/Ollama/Local CLI transport catches, sleep timing, and logging execution.
- macOS `AIService`, `OllamaService`, and `APIKeyManagementView` read/write dynamic Ollama, Custom provider, OpenRouter model cache preferences, provider settings chrome, provider connection status presentation, Ollama form labels/failure copy, Ollama service diagnostic copy, and Custom provider form labels/submit enablement through `VoiceInkDynamicAIProviderPreference`/`VoiceInkAIEnhancementProviderSettingsPresentation`/`VoiceInkOllamaServiceDiagnostics`; `OllamaService` reads the default base URL directly from `VoiceInkPreferenceDefault`, reads its legacy selected-model fallback and request temperature from `VoiceInkAIEnhancementProviderKind`, and delegates refresh-time selected-model repair plus refresh-plan persistence application to `VoiceInkAIEnhancementModelRefreshPlan`/`VoiceInkDynamicAIProviderPreference`, while macOS still owns the dynamic-provider clients, one SwiftUI status-color adapter, and live model refreshes.
- macOS `LocalCLIService`, `AIService`, and `APIKeyManagementView` read Local CLI templates, settings labels/help, command preference, selected-template preference, timeout default/options/clamp, configured-command predicate, full-prompt wrapper, stdout/stderr cleanup, command-failure classification, and execution error presentation from `VoiceInkLocalCLITemplate`/`VoiceInkLocalCLIPreference`/`VoiceInkLocalCLIExecutionError`, while macOS still owns `/bin/zsh` execution, interactive PATH discovery, process timeout/termination, pipe IO, and environment injection.
- macOS and iOS provider API-key lookup delegates stored-key reference resolution and provider environment fallback policy to `VoiceInkProviderAPIKeyLookup`; typed iOS provider callers use `VoiceInkProviderKind`, while dynamic macOS provider-name callers keep the string overload. macOS still owns its Keychain adapter and UI editing state; iOS provider-key storage routes through shared core.
- macOS and iOS provider API-key storage uses `VoiceInkKeychainQuery`, `VoiceInkKeychainDataStore`, and `VoiceInkKeychainValueStore` for the shared app service, account, syncability, data-protection, UTF-8 string value encoding/loading, add, copy-data, delete, delete-success status, and exists operation shape; iOS provider account routing, save/delete calls, and save-failure diagnostic copy use `VoiceInkProviderAPIKeyStorage`, while the macOS adapter still owns local-build fallback storage, logging, refresh, and UI editing state. Provider API-key screens use `VoiceInkSecretPresentation`, colocated with provider-key form policy, for stored-key display obfuscation and placeholder fallback text, and `VoiceInkProviderAPIKeyVerificationApplicationPlan` for success/failure/save decisions while keeping platform-specific UI side effects.
- macOS license startup, diagnostics, Polar adapter, secure-storage adapter, and license-management presentation route non-sensitive license preference keys, license startup/trial lifecycle, validation feedback/application planning, purchase/management URL policy, support-link metadata, activation/deactivation copy, trial-banner presentation, feature copy/icons, removal/reset planning, launch flag storage, activation-limit storage, stored-license access predicates, fallback device-id storage, license Keychain account names, trial-start timestamp coding, device-local syncability, Polar endpoint/request-body DTOs, granted-status checks, activation-required checks, and HTTP error classification through `VoiceInkLicensePreference`/`VoiceInkLicenseStartupPolicy`/`VoiceInkLicenseValidationPolicy`/`VoiceInkLicenseLinks`/`VoiceInkLicenseManagementPresentation`/`VoiceInkLicenseTrialBannerPresentation`/`VoiceInkLicenseRemovalPolicy`/`VoiceInkLicenseSecureStorageAccount`/`VoiceInkLicenseSecureStoragePolicy`/`VoiceInkLicenseServicePolicy`; Keychain execution, serial-number lookup, Polar `URLSession` execution, AppKit URL opening, notification posting, SwiftUI colors/layout, and license UI state remain in the macOS shell. The obsolete raw-copy `LicenseView.swift` stays deleted so license UI enters through the shared-presentation `LicenseManagementView`.
- macOS sidebar/window titles read `VoiceInkAppIdentity`, macOS main-view sidebar labels/icons, Power Mode visibility filtering, empty-selection copy, and notification-to-view selection mapping route through `VoiceInkMacOSMainViewItem`, and macOS menu-bar visibility, dock-icon hiding storage/defaults, menu labels, empty-state copy, dynamic selection labels, and picker/checkmark symbols route through `VoiceInkMenuBarPreference`/`VoiceInkMacOSMenuBarPresentation`; app identity presentation and UI preference keys stay shared while Xcode product metadata, SwiftUI layout, destination rendering, menu actions, and AppKit activation policy remain platform shell behavior.
- macOS and iOS recording audio filename construction plus failed-delete log copy delegates live recording, imported transcription, retranscription, and timestamped iOS recording naming to `VoiceInkStoredAudioFile`; platform shells still own directory choice and actual audio capture/copy/write/delete work.
- macOS and iOS live-recording rows build pending and canceled records through `VoiceInkRecordingTranscriptionDraft` factories, colocated with completed transcription draft construction; canceled-record mutation routes through `VoiceInkTranscriptionRecordCancellationPlan`; platform shells still own recording lifetime, SwiftData insertion, and save/notification timing.
- macOS and iOS `Transcription` SwiftData models conform to `VoiceInkStoredAudioRecord`, so record-level audio URL resolution, availability checks, missing-audio fallback text, positive-duration missing-path visibility, existence checks, and deletion use the same shared interface while each shell still supplies its recording directory.
- macOS open-file routing, audio-file transcription queue validation, queue status/phase policy, queue action/drop copy/icons, queue mutation policy, cancel-reset policy, supported-file help text, open-panel content types, drag/drop type identifiers, enabled-only Power Mode metadata, completed audio-file draft construction, and audio-file enhancement success/failure completion delegate supported audio/video extension policy and presentation to `VoiceInkSupportedMedia`/`VoiceInkAudioFileQueueStatus`/`VoiceInkAudioFileQueuePolicy`/`VoiceInkAudioImportPresentation`/`VoiceInkAudioFileQueuePresentation` plus `VoiceInkPowerModeTranscriptionMetadata`/`VoiceInkAudioFileTranscriptionDraft`; platform shells still own open-panel execution, drag/drop provider loading, queue storage mutation, active Power Mode lookup, enhancement execution, persistence, queue failure logging, and transcription execution.
- macOS transcript export delegates CSV header/row formatting/escaping plus text-save suggested filenames, export file extensions, localized timestamp creation, and Markdown body formatting to the shared transcript export module through `VoiceInkTranscriptionCSVExporter` and `VoiceInkTranscriptFileExport`; common transcript copy/save control copy and icons route through `VoiceInkTranscriptPresentation`; macOS still owns `NSSavePanel`, mapping SwiftData `Transcription` records into export records, clipboard writes, animations, and file writes.
- macOS transcript details, CSV export, and Power Mode row selected-language, trigger-count badges, row-detail chips, chip symbols/accent style, panel/sidebar/popover chrome copy, config form section labels/defaults/actions, app-picker search copy, delete-confirmation copy, validation-alert copy, no-transcription-models form copy, AI-enhancement empty-state form copy, AI-enhancement form chrome copy, advanced-form chrome copy, and emoji input/action/icon/help/blocked-removal feedback delegate power-mode display formatting to `VoiceInkPowerModePresentation`/`VoiceInkPowerModeEmojiInputPresentation`.
- macOS and iOS local Whisper model directory creation, local `.bin` listing, download URL/file path lookup, runtime selected-model file resolution, imported-local-model URL resolution, downloaded-local-model record lookup, imported-local-model merge planning, final downloaded-file installation plus local-model record construction, bootstrap-model availability, Core ML support checks, model deletion, download progress keys/math, percent text, compact status text, iOS download lifecycle tracking, iOS downloaded/download-progress state assembly, iOS download completion install/failure/cancellation planning, iOS onboarding/local-model management row assembly, iOS onboarding primary-action planning, local-model row/action presentation and action affordances, iOS downloaded-model delete request planning, local-model operation confirmation copy, local-model operation alert copy, and iOS local-model management diagnostic copy delegate to `VoiceInkWhisperModelFiles`/`VoiceInkWhisperModelDownloadProgress`/`VoiceInkWhisperModelSimpleDownloadTrackingState`/`VoiceInkWhisperModelSimpleDownloadCompletionPlan`/`VoiceInkWhisperModelDownloadState`/`VoiceInkWhisperModelDeletionPolicy`/`VoiceInkWhisperModelManagementList`/`VoiceInkWhisperModelDownloadRowPresentation`/`VoiceInkOnboardingModelDownloadPrimaryAction`/`VoiceInkWhisperModelOperationConfirmationPresentation`/`VoiceInkWhisperModelOperationAlertPresentation`/`VoiceInkWhisperModelManagementDiagnostics`; macOS local-model import planning for importability, model name, destination filename/URL, duplicate detection, and local-model record construction also uses `VoiceInkWhisperModelFiles`, while platform shells still own URLSession task state, UI, runtime model state, file copy execution, OSLog delivery, file-panel execution, and concrete `TranscriptionModel` construction.
- macOS and iOS model-management filter labels, recommended model ordering, local/cloud/custom filter membership, default-model status copy, model-card speed/accuracy/action/badge labels, model delete-confirmation copy, imported-local-model copy, local/cloud settings titles, local/cloud manage actions, Intel-local-model warning copy, and custom/local model-management copy delegate to `VoiceInkModelManagementFilter`/`VoiceInkModelManagementModelFacts`/`VoiceInkModelManagementPresentation`; macOS local-model import panel/help/notification copy also comes from `VoiceInkModelManagementPresentation`, while platform shells still own navigation, layout, provider configuration, and model operation execution.
- macOS waveform playback and iOS note audio playback use `VoiceInkAudioPlaybackTimeline` for clamped progress, seek math, update cadence, and completion tick planning, `VoiceInkAudioPlaybackTimerTickPlan` for timer stop/seek hints, `VoiceInkAudioPlaybackState` for load/play/pause/stop/seek/rate/tick state transitions and tick-plan application, `VoiceInkAudioPlaybackRate` for the existing `audioPlaybackRate` storage/cycling/label policy, `VoiceInkIOSAudioPlaybackSessionConfiguration` for the iOS playback category policy in the shared iOS audio configuration surface, `VoiceInkAudioPlaybackPresentation` for loading/play-pause/metadata-icon presentation plus macOS audio-player action help copy, and `VoiceInkAudioPlaybackDiagnostics` for iOS load/play failure log text plus macOS waveform/load failure console copy; platform shells still own `AVAudioPlayer`, audio-session execution/adaptation, timer/seek execution, OSLog delivery, hover/drag gestures, and rendering.
- macOS history/audio-file rows, iOS note/audio rows, and stored-audio missing-path presentation use `VoiceInkDurationPresentation.shouldShowPositiveDuration` before rendering duration-dependent UI, with shared metadata separator text; platform shells still own layout and final formatting placement.
- macOS and iOS local Whisper wrappers read `VoiceInkWhisperContextRuntimePlan` and apply it through `VoiceInkWhisperContextParameterSink` before initializing whisper.cpp contexts, so simulator CPU mode, device flash-attention enablement, diagnostic message selection, context parameter assignment, and runtime failure diagnostic copy stay in shared core. Those wrappers also use `VoiceInkWhisperRuntimeInvocationPlan` and the shared runtime parameter-sink Interface before `whisper_full`, so optional language, prompt, VAD model path pointer lifetime, and full/VAD parameter assignment share one policy while C execution stays platform-owned.
- macOS and iOS local Whisper and macOS rolling preload resolve the Silero VAD bundle resource directly through `VoiceInkVADModelFiles`, colocated with `VoiceInkWhisperModelFiles`; the old shell-only VAD model manager adapters were removed.
- macOS `FluidAudioModelManager`, `FluidAudioModelCardView`, `FluidAudioTranscriptionService`, and `FluidAudioStreamingProvider` route model-download status copy/percent text and local ASR trailing-silence padding through `VoiceInkFluidAudioDownloadStatus`/`VoiceInkFluidAudioTranscriptionPolicy`; the streaming provider also delegates immediate-pass scheduling, pass eligibility, seek math, and cached-final hypothesis freshness to the same core Module while retaining FluidAudio SDK progress mapping, runtime calls, buffer locks, logging, and event delivery in the macOS adapter. `FluidAudioWordTimingAdapter` remains a macOS shell adapter because it imports the FluidAudio token type.
- macOS streaming sessions choose file/streaming mode, FluidAudio versus cloud streaming adapter, rolling-preload quick release, and final-commit wait time through `VoiceInkTranscriptionSessionRouteFacts`, `VoiceInkStreamingFinalCommitTimeout`, and `VoiceInkTranscriptionSessionExecutionPlan`; concrete Whisper, Native Apple, FluidAudio, cloud runtime services, and file/streaming session instantiation remain macOS-owned.
- macOS wake/app-launch model prewarm, recording-start local runtime loading, local Whisper runtime updates after model selection, and current-model deletion cleanup use `VoiceInkTranscriptionRuntimeResourcePlan`/`VoiceInkLocalWhisperRuntimeUpdate`/`VoiceInkTranscriptionModelDeletionPlan`; macOS still owns wake notifications, bundled prewarm audio, concrete Whisper context loading, FluidAudio model loading, and actual model-file deletion.
- macOS cloud streaming providers use `VoiceInkStreamingTranscriptionEvent` and `VoiceInkStreamingTranscriptionError` directly from the shared streaming taxonomy, and connect through `VoiceInkTranscriptionModelProvider.streamingConnectionModelName` for provider-specific runtime model names, including ElevenLabs/Soniox realtime aliases, Speechmatics operating points, and the Mistral realtime model alias; provider-specific transport-timeout mapping comes from `VoiceInkTranscriptionModelProvider.mapsStreamingTransportTimeoutToFinalTimeout`; `StreamingTranscriptionProvider` keeps the single `LLMkit` event/error forwarding adapter while FluidAudio emits local ASR events directly.
- macOS `TranscriptionServiceRegistry`, `TranscriptionModel`, streaming model-card toggles, and streaming-key migration use `VoiceInkTranscriptionStreamingPreference`/`VoiceInkTranscriptionStreamingModePresentation`; macOS still adapts `TranscriptionModel` and `CloudProvider` into the shared streaming preference and session-route snapshots.
- macOS `CloudModelCardView` uses `VoiceInkProviderAPIKeyCardPresentation` for API-key card copy/icons and the shared provider-key verification tone through one macOS SwiftUI adapter; provider-key verification persistence now applies through `APIKeyManager`, while SwiftUI layout, model-list refresh, expansion animation, and verification task orchestration stay in the macOS shell.
- macOS startup calls `VoiceInkStreamingKeysMigration` directly before model loading, so old streaming flags and removed realtime model names are repaired in shared core while app launch ordering remains macOS-owned.
- macOS onboarding welcome uses `VoiceInkMacOSOnboardingPresentation.welcome` for title, subtitle, button titles, and typewriter roles while keeping layout, animation, and onboarding navigation in the macOS shell.
- macOS onboarding model download uses `VoiceInkMacOSOnboardingPresentation.modelDownload` for title, subtitle, metric labels, and button title policy while keeping FluidAudio model lookup, download execution, default-model selection, and animation in the macOS shell.
- macOS onboarding tutorial uses `VoiceInkMacOSOnboardingPresentation.tutorial` for title, subtitle, shortcut label, instruction steps, completion/skip buttons, and placeholder copy/icon while keeping shortcut rendering, focus/text-entry state, completion action, and animation in the macOS shell.
- macOS settings reset-onboarding alert uses `VoiceInkMacOSOnboardingPresentation.resetSettingsAlert` for button, alert, action, and message copy while keeping onboarding-state mutation in the macOS shell.
- macOS onboarding permission steps use `VoiceInkMacOSOnboardingPermissionPresentation` for step order, copy, screen-context help, audio-device selection copy, relaunch copy, skip title/eligibility, and button titles while keeping permission checks, timers, device selection, shortcut recording, and animations in the macOS shell.
- macOS `PermissionsView` uses `VoiceInkMacOSPermissionSettingsPresentation` for permission settings header/card copy, icons, relaunch copy, and button title policy while keeping permission checks, polling, relaunch action, and SwiftUI layout in the macOS shell.
- macOS rolling-buffer preload settings labels/help, rolling VAD model selection, partial-transcript preview notification contract, backup import/export payload shape, app defaults, settings UI, per-model model-card toggles, diagnostics including current-model preload state, coordinator preload gates, and lead-in retention use `VoiceInkRollingAudioBuffer`, `VoiceInkRollingBufferPreloadSettings`, `VoiceInkRollingBufferPreloadPartialTranscriptRequest`, `VoiceInkRollingBufferVADSettings`, `VoiceInkRollingBufferBackupPreferences`, `VoiceInkRollingBufferBackupImportPlan`, and `VoiceInkRollingBufferPreloadPolicy` directly; max-byte trimming, chunk/data snapshots, backup import normalization/clamping, rolling VAD model validation, and per-model preload restore/export also live in shared core, while macOS still owns IOKit power-state reads, VAD startup/execution, rolling audio source delivery, settings-change notification posting, and quick-release diagnostics storage.
- macOS special shortcut empty-tap fallback delegates short-press and empty-completed-transcript eligibility to `VoiceInkSpecialShortcutEmptyFallbackPolicy`; `LastTranscriptionService` still owns SwiftData lookup and cursor paste.
- macOS special shortcut key-up handling delegates unsafe key-evidence discard decisions to `VoiceInkSpecialShortcutKeyEvidencePolicy`, colocated with the special shortcut fallback policy; `ShortcutMonitor` still owns macOS keyboard event collection and now passes `VoiceInkShortcutPressContext` directly.
- macOS recorder state, capture/processing/dismiss/pipeline-finish predicates, processing-status copy/timing, UI toggle actions, and stale hidden-recorder session repair now use `VoiceInkRecordingState`, `VoiceInkRecorderProcessingPresentation`, `VoiceInkRecorderUIToggleAction`, and `VoiceInkRecorderUISessionPolicy` directly; `VoiceInkEngine` builds recording rows through `VoiceInkRecordingTranscriptionDraft`, colocated with completed transcription draft construction, and `RecordingShortcutManager`, `RecorderUIManager`, rolling preload, AppDefaults, diagnostics, and the recorder-style settings picker/labels use shared state/style gates while macOS owns recorder UI/window execution.
- macOS audio input storage and settings controls use `VoiceInkAudioInputMode`/`VoiceInkAudioInputPreference`/`VoiceInkMacOSAudioDeviceChangeRequest`/`VoiceInkMacOSAudioInputSettingsPresentation`/`VoiceInkAudioInputPriorityDevice`/`VoiceInkAudioInputPriorityPolicy`/`VoiceInkAudioInputAutomaticSelectionPolicy` for input mode raw values/default/presentation, storage keys/default registration, selected-device UID persistence, last-used microphone notification suppression, audio-device change/switch notification names and payload key, settings copy/icons, persisted device shape, add/remove/reindex/move, sorted priority order, first-available priority selection, and safe automatic input selection while keeping CoreAudio discovery, transport/UID adaptation, selected-device side effects, persistence timing, and SwiftUI layout in the macOS shell.
- macOS `AppDefaults` registers core-owned default values through `VoiceInkDefaultSettings.macOS`, checks first-run onboarding storage through `VoiceInkOnboardingPreference`, and runs macOS startup legacy-preference migrations through `VoiceInkStartupPreferenceMigration` colocated with UserDefaults preference policy, while keeping macOS-specific defaults such as current model, Launch at Login, shortcuts, and UI behavior in the shell.
- iOS app launch registers core-owned default values through `VoiceInkDefaultSettings.iOS` and runs iOS legacy-preference migrations through `VoiceInkStartupPreferenceMigration` colocated with UserDefaults preference policy, and macOS/iOS storage startup failure copy routes through `VoiceInkStorageStartupDiagnostics`, while the platform shells keep SwiftData container construction, first-run flow, App Group defaults/Darwin notification execution, URL handling/opening, and file deletion.
- macOS history/import row summaries, macOS history delete-confirmation copy, macOS audio-file row copy/save action text, macOS common transcript copy/save button copy/icons, macOS transcript original/enhanced tab labels, tab visibility, tab-selected display text, and iOS note detail display/retry labels plus copy/retry action icons and retry-control state use `VoiceInkTranscriptPresentation`/`VoiceInkTranscriptTextVariant` for the enhanced-text-first transcript display rule, expanded/collapsed action-text policy, shared empty-content fallback copy, pending-vs-failed retry progress/button policy, status error-detail visibility, and shared transcript-detail presentation copy.
- macOS full and inline history empty/no-selection/no-metadata states plus shortcut-tip copy/icon, search prompts, load-more/loading text, selection labels, action labels/icons, selected-count text, delete-alert button/title copy, console diagnostic copy, SwiftData history query descriptor construction, pagination/selection/delete state policy, and search/latest-record refresh decisions use `VoiceInkHistoryPresentation`/`VoiceInkHistoryDiagnostics`/`TranscriptionHistoryQuery`/`VoiceInkHistoryPaginationPolicy`/`VoiceInkHistorySelectionPolicy`/`VoiceInkHistoryDeletionPolicy`/`VoiceInkHistoryRefreshPolicy` while keeping SwiftUI layout, shortcut recording/rendering, SwiftData fetch/delete execution, audio-file deletion, and reload task execution in the macOS shell.
- macOS transcription detail metadata labels/icons, AI request section labels, section visibility, and copy-button payload composition use `VoiceInkTranscriptionMetadataPresentation` while keeping SwiftUI row layout and clipboard rendering in the macOS shell.
- macOS recorder failure records adapt `VoiceInkTranscriptionRecordFailurePlan` into the existing raw-status SwiftData model, preserving the `Transcription Failed: ...` stored text prefix without duplicating failed status/text decisions in the pipeline.
- macOS canceled recorder records adapt `VoiceInkTranscriptionRecordCancellationPlan` into the existing raw-status SwiftData model, preserving canceled text, optional duration/model updates, and enhancement/request metadata clearing without owning cancellation policy in the shell.
- macOS live recording applies completed transcript, enhancement success/failure metadata, and completed status through `VoiceInkCompletedTranscriptionDraft`; audio-player re-enhancement still applies AI enhancement metadata through `VoiceInkMutableTranscriptionEnhancementRecord`/`VoiceInkMutableTranscriptionEnhancementMetadataRecord`, keeping the enhancement text/model/duration/prompt/request-field mutation in shared core while the macOS shell still owns AI execution, notifications, persistence timing, and banners.
- `VoiceInkTranscriptPresentation.matchesSearch` mirrors macOS history predicate search semantics, keeping iOS note filtering accent-insensitive through shared core while macOS SwiftData predicate construction stays in the `TranscriptionHistoryQuery` shell adapter.
- macOS last-transcription paste eligibility/notification copy and iOS note-row/detail status presentation use `VoiceInkTranscriptPresentation` defaults for canceled/empty/pending/failed transcript presentation, status panel visibility, completed-content visibility, status symbol/tone metadata, inline progress visibility, note-detail retry-control state, status error-detail visibility, and row badge state; iOS SwiftUI tone colors live in one app-shell adapter, while platform shells still own final color rendering, retry task execution, clipboard/cursor paste, notification dispatch, and paste execution.
- macOS last-transcription candidate selection, fetch window, fetch-failure diagnostic copy, original-vs-preferred copy/paste text selection, copy/retry notification presentation, and retry-preflight error presentation route through `VoiceInkLastTranscriptionPolicy`; macOS still owns SwiftData fetch execution, clipboard/cursor paste, notification dispatch, and retry execution.
- macOS `FillerWordManager`, iOS `AppSettings`, and cleanup configuration load and save filler-word lists through `VoiceInkFillerWordPreference`; macOS and iOS setting surfaces use `VoiceInkFillerWordDraftState`/`VoiceInkFillerWordDraftSubmission` plus `VoiceInkFillerWordEditorPresentation` for draft text, add-button availability, inserted-word normalization, draft reset, duplicate-alert presentation, editor/list visibility, and changed-list persistence planning while platform storage shells only apply the shared optional update and still own settings UI and toggle state.
- iOS dictionary settings plus macOS dictionary form, section-card, hero, settings-panel, shortcut labels, quick-add panel labels/placeholders/hints, word-replacement guidance, edit-sheet labels, list headers, sort help, row action help, vocabulary draft text/submit/reset/duplicate-alert planning, word-replacement draft text/visibility/submit/reset/duplicate-alert planning, word-replacement edit state/saveability/update-completion planning, accepted-submission list application, changed-value persistence planning, sort mode storage/toggle/indicator policy, vocabulary/word-replacement list sorting, and sorted-row deletion planning consume `VoiceInkDictionarySettingsPresentation`/`VoiceInkDictionaryQuickAddPresentation`/`VoiceInkWordReplacementInfoPresentation`/`VoiceInkWordReplacementEditPresentation`/`VoiceInkVocabularyListPresentation`/`VoiceInkWordReplacementListPresentation`/`VoiceInkVocabularyDraftState`/`VoiceInkVocabularyDraftSubmission`/`VoiceInkVocabularySubmissionPlan`/`VoiceInkWordReplacementDraftState`/`VoiceInkWordReplacementDraftSubmission`/`VoiceInkWordReplacementSubmissionPlan`/`VoiceInkWordReplacementEditState`/`VoiceInkWordReplacementEditSubmission`/`VoiceInkDictionaryListSortPreference`/`VoiceInkDictionaryListSortPolicy`; macOS quick-add consumes the same vocabulary and word-replacement draft states, macOS edit sheet consumes shared edit state/submission, and iOS word-replacement rows also consume shared arrow presentation while platform shells still own SwiftUI layout, button visibility, settings bindings, shortcut actions, panel sizing/focus, sheet presentation, and SwiftData/UserDefaults persistence.
- iOS app launch and first-time setup read/write/reset onboarding completion through `VoiceInkOnboardingPreference`, with startup app-settings state loaded by `VoiceInkIOSAppSettingsStartupPolicy` and first-time default-mode repair/onboarding-completion write intent planned by `VoiceInkIOSFirstTimeSetupPolicy`; iOS onboarding step order, copy/features/steps, app-icon bundle metadata extraction/source/fallback decision, and fallback symbol route through `VoiceInkIOSOnboardingStep`, `VoiceInkIOSOnboardingPresentation`, and `VoiceInkIOSAppIconPolicy`; the onboarding views, animations, `Bundle`/`UIImage` icon loading, model-download action, completion write execution, recording-after-onboarding flow, and Keychain/UserDefaults execution adapters stay in the iOS shell. macOS metrics setup step order, icons, labels, help text, and action-title policy use `VoiceInkMacOSSetupPresentation`, while permission checks and navigation stay in the macOS shell.
- iOS `AppSettings` reads/writes/resets audio-session timeout through `VoiceInkAudioSessionTimeoutPreference`; that shared preference Module also owns timeout range, step, display text, settings presentation copy, immediate-vs-delayed deactivation plan, shell execution intent, and countdown tick policy. `AudioSessionManager` routes recording category/mode/options through `VoiceInkIOSAudioSessionRecordingConfiguration`, and routes active/remaining-time transitions plus log text through `VoiceInkAudioSessionLifecycleState` and `VoiceInkAudioSessionDiagnostics` in the shared iOS audio configuration surface, while AVAudioSession activation/deactivation execution, AVFoundation option adaptation, Timer scheduling, OSLog delivery, and timeout UI rendering stay in the iOS shell.
- iOS `AppSettings` loads/saves/deletes provider API keys through `VoiceInkProviderAPIKeyStorage`, and the iOS API-key form routes stored-key edit/reset planning, verified-state feedback visibility, plus save/verify control presentation through `VoiceInkProviderAPIKeyFormState`, so provider-to-Keychain account mapping, skipped local/bundled providers, default empty loads, Keychain status failure detection/diagnostics, verification-flag reset on edit, verification-result display gating, and progress-vs-button/disabled-state decisions live in shared core while the iOS shell keeps observable settings state, SwiftUI rendering, and logging delivery.
- iOS `AppSettings.resetAll()` restores in-memory reset values and provider-key deletion targets from `VoiceInkDefaultSettings.iOS.appSettingsResetState` and clears persisted core settings through `VoiceInkSharedPreferenceReset`, including modes, onboarding, verification flags, transcription prompt/language/model settings, cleanup settings, AI-enhancement provider/model settings, dynamic provider caches, custom prompts, VAD, dictionary sort preferences, and filler-word overrides. iOS debug reset order, file cleanup, and SwiftData reset failure diagnostic copy route through `VoiceInkAppDataResetPlan`/`VoiceInkAppDataResetFilePlan`/`VoiceInkAppDataResetDiagnostics`, while the iOS shell still owns SwiftData note deletion and settings-reset execution.
- macOS transcription services, Power Mode, rolling preload, and iOS `AppSettings` read/write/clear selected transcription language through `VoiceInkTranscriptionLanguagePreference`; macOS fallback language selection, Native Apple source-compatible fallback, and language-selection copy live in shared core while platform shells still own settings UI and language-change notifications.
- macOS language selection control state, Native Apple asset-control visibility, Native Apple language asset-control help/accessibility/icon presentation, and compatible-language fallback come from `VoiceInkTranscriptionLanguageSelectionFacts`/`VoiceInkNativeAppleLanguageAssetPresentation`; macOS still owns SwiftUI rendering, Speech asset checks/install execution, prompt refresh, and language-change notifications.
- macOS Native Apple transcription formats asset-required language names through `VoiceInkLanguageCatalog.nativeAppleDisplayName`, while Speech framework availability, asset checks, and asset installation remain macOS shell responsibilities.
- macOS `TranscriptionModel` exposes shared language options, provider-role category/route/availability/streaming policy, AssemblyAI realtime/batch language selection, and fallback repair through `VoiceInkTranscriptionLanguageSupport`/`VoiceInkTranscriptionModelProviderRole`; macOS still adapts `ModelProvider`, keeps custom-model stored language lists, and supplies per-model streaming preference state. The standalone `TranscriptionLanguageSupport` wrapper is deleted.
- macOS custom cloud model setup uses `VoiceInkCustomCloudModelFormPresentation` for form defaults, labels, placeholders, action icons, default model description, and Keychain failure copy, while `AddCustomModelView` keeps SwiftUI state, expansion animation, and persistence side effects.
- macOS custom cloud model persistence delegates its UserDefaults key, JSON load/save loop, Codable stored shape, and legacy inline `apiKey` migration eligibility to `VoiceInkCustomCloudModelStorage`/`VoiceInkCustomCloudModelStoredRecord`; macOS still adapts to `CustomCloudModel` and owns Keychain writes.
- macOS settings backup category taxonomy, ordered category titles, import/export panel copy, import status/error diagnostic copy, save-failure import error, alert titles/messages, version-mismatch warning, import summary text, API-key-reminder gate, and restart recommendation use `VoiceInkSettingsBackupCategory`/`VoiceInkSettingsBackupImportPolicy`/`VoiceInkSettingsBackupImportDiagnostics`/`VoiceInkSettingsBackupImportError`/`VoiceInkSettingsBackupPresentation`; macOS still owns AppKit import/export panels, JSON file IO, concrete settings side effects, and settings-tab navigation.
- macOS general-settings backup preference grouping, import-plan routing, and portable preference write application use `VoiceInkGeneralSettingsBackupPolicy` for cleanup settings, experimental recording-feedback state, paste behavior, and rolling-buffer settings; macOS still owns the concrete JSON wire fields plus shortcut, launch/menu/recorder, live feedback-controller, notification, and persistence side effects.
- macOS custom cloud model backup records use `VoiceInkCustomCloudModelBackup`/`VoiceInkCustomCloudModelImportPlan` for the Codable export/import shape, endpoint/model-name import normalization, imported model rebuild fields, and API-key restore eligibility; macOS still adapts to `CustomCloudModel` and owns Keychain writes.
- macOS dictionary backup import/export uses `VoiceInkVocabularyWordBackup`, `VoiceInkDictionaryBackupExportPlan`, and `VoiceInkDictionaryPolicy.dictionaryBackupImportPlan`/`dictionaryBackupExportPlan` for the Codable vocabulary shape, nil-vs-empty export decisions, word-replacement duplicate export replacement policy, shared vocabulary trim/duplicate import policy, word-replacement duplicate skipping, invalid-row counting, trimmed import records, save/no-save decision, and word-replacement cache invalidation decision; macOS still adapts SwiftData `VocabularyWord`/`WordReplacement` rows and owns persistence execution/cache invalidation.
- macOS paste/pipeline, audio-file transcription, Power Mode, backup import/export, `FillerWordManager`, and iOS `AppSettings` read/write/reset cleanup toggles through `VoiceInkTranscriptionCleanupPreferenceStorage` and `VoiceInkTranscriptionCleanupSettings`; cleanup backup export shape and legacy `removePunctuation` import fallback also live in `VoiceInkTranscriptionCleanupSettings`. macOS/iOS cleanup settings labels and help text come from `VoiceInkTranscriptionCleanupPresentation`, while each shell keeps SwiftUI rendering and direct `@AppStorage`/settings bindings. macOS transcription paths and iOS retry share `VoiceInkPreparedTranscriptionText` for filtered text, word-replacement input, and final cleaned output.
- macOS Power Mode transcription model and language controls delegate selected-model fallback, disabled autodetect, multilingual picker vs hidden default, compatible language repair, and provider-source fact classification to `VoiceInkPowerModeTranscriptionSelection`/`VoiceInkPowerModeTranscriptionModelFacts`/`VoiceInkPowerModeTranscriptionModelResourceFacts`; macOS still adapts `TranscriptionModel` into shared facts and renders the SwiftUI controls.
- macOS Power Mode emoji selection delegates the default emoji catalog, custom emoji storage key, input validation, add/remove mutation policy, picker input-draft feedback, submit-result feedback, picker action/icon/help copy, feedback severity, and in-use deletion alert presentation to `VoiceInkPowerModeEmojiCatalog`/`VoiceInkPowerModeEmojiInputPresentation`; macOS still owns picker rendering, focus, usage lookup, and alert execution.
- iOS `AppSettings` stores word-replacement rules through `VoiceInkWordReplacementPreference`, applies accepted submissions through `VoiceInkWordReplacementSubmissionPlan`, deletes settings rows through `VoiceInkPreferenceList` from the shared UserDefaults preference policy, and passes current stored rules into `VoiceInkTranscriptionRunProcessor`; shared core owns word-replacement draft state plus validation and trimming through `VoiceInkDictionaryPolicy`, and `VoiceInkWordReplacementEngine` owns rule ordering and replacement semantics, while macOS still owns SwiftData dictionary storage and cache invalidation.
- iOS `AppSettings` stores custom vocabulary terms through `VoiceInkCustomVocabularyPreference`, applies accepted submissions through `VoiceInkVocabularySubmissionPlan`, deletes settings rows through `VoiceInkPreferenceList` from the shared UserDefaults preference policy, and passes current stored terms into `VoiceInkTranscriptionRunProcessor`; shared core owns vocabulary draft state and provider/use-specific vocabulary normalization before `VoiceInkAudioTranscriptionService`, while macOS still owns SwiftData vocabulary storage and streaming dictionary adapters.
- macOS `TranscriptionPipeline` reads short post-processing skip settings through `VoiceInkPostProcessingSkipConfiguration.current()` before applying `VoiceInkPostProcessingSkipPolicy`, colocated with transcription run preparation; `EnhancementSettingsView`, `EnhancementSettingsPanel`, and `EnhancementShortcutsView` use `VoiceInkEnhancementSettingsPresentation` for outer enhancement toggles/sections/action chrome, shortcut row labels/help/docs/key-chip copy, and short-skip labels/options while the macOS shell still owns rendering, bindings, and the decision point.
- macOS `VoiceInkEngine` uses `VoiceInkMacOSRecordingCancellationPolicy` for capture, post-processing, idle, and busy cancellation decisions while keeping recorder stop, session teardown, SwiftData writes, and cleanup in the shell. `VoiceInkEngine` and `Recorder` use `VoiceInkRecordingPermissionPolicy` for start/request/denied recording-permission action planning and `VoiceInkRecordingNotificationPresentation` for no-model, failed-start, microphone-permission, and runtime failure recording notification title/duration/action copy while keeping recorder state transitions, OS permission queries/requests, permission-opening actions, and macOS notification dispatch in the shell. iOS also routes permission settings-open fallback decisions through `VoiceInkRecordingPermissionPolicy`, while `RecordingManager` keeps `UIApplication` URL construction and opening in the shell.
- macOS `SessionMetricRecorder` and `SessionMetricMigrationService` build metric row drafts, recorder source, completed-status filtering, word count, positive durations, speed factor, and stats-migration completion storage through `VoiceInkSessionMetricPolicy`/`VoiceInkSessionMetricDraft`/`VoiceInkSessionMetricMigrationPreference`; macOS still owns duplicate-record checks, SwiftData insertion, migration scheduling, and metric notifications, while iOS records now conform to the same metric source interface for future stats.
- macOS `MetricsContent` accumulates dashboard words/duration and derives time saved, words per minute, average-WPM display text, keystrokes saved, hero/empty-state copy, stat-card labels/icons/details, model-performance action presentation, promotion visibility, and help/resource link copy/icons/URLs through `VoiceInkDashboardMetrics`/`VoiceInkDashboardPresentation`/`VoiceInkDashboardPromotionPresentation`/`VoiceInkHelpResourcesPresentation` consuming `VoiceInkLicenseState`; the macOS trial banner message, severity threshold, title, action copy, and SF Symbol route through `VoiceInkLicenseTrialBannerPresentation`. macOS still owns SwiftData loading, cache lifetime, accessibility status, dashboard rendering, navigation execution, purchase-link opening, support-email opening, and trial-banner colors/layout.
- macOS metrics panels delegate transcription/enhancement model grouping, averages, speed-factor calculation, speed-factor display text, average-processing-time display text, performance-panel copy/icons, and model-performance time-filter storage/date windows directly to `VoiceInkPerformanceAnalyzer`/`VoiceInkPerformancePresentation`/`VoiceInkPerformanceTimeFilter`; iOS note summaries consume the same performance policy through `VoiceInkNoteListSummaryPresentation`. Shared performance records now use the stored `transcriptionModelName` and `aiEnhancementModelName` fields directly instead of shell-only model-name aliases. macOS still owns SwiftData queries, system-info lookup, and SwiftUI rendering, while iOS owns its note-list rendering.
- macOS `AIEnhancementService` reads request timeout and retry-on-timeout through `VoiceInkAIEnhancementRequestPreference`; `EnhancementSettingsPanel` uses `VoiceInkEnhancementSettingsPresentation` for request-timeout labels/options while SwiftUI setting controls still bind directly through `@AppStorage`.
- macOS `TranscriptionModelManager`, `PowerModeConfig`, and `SystemInfoService` use `VoiceInkCurrentTranscriptionModelPreference` for the selected transcription model; `TranscriptionModelManager` delegates current-model load restore/clear decisions, usable-model eligibility, requirement predicates, downloaded local Whisper model lookup, selected-language repair planning, local Whisper runtime updates, and current-model deletion cleanup to `VoiceInkCurrentTranscriptionModelLoadPlan`/`VoiceInkTranscriptionModelAvailabilityFacts`/`VoiceInkWhisperModelFiles`/`VoiceInkTranscriptionLanguageRepairPlan`/`VoiceInkTranscriptionRuntimeResourcePlan`/`VoiceInkLocalWhisperRuntimeUpdate`/`VoiceInkTranscriptionModelDeletionPlan`, while still supplying macOS download, API-key, and OS-availability facts. `VoiceInkEngine` also uses `VoiceInkWhisperModelFiles` for recording-startup local Whisper preload lookup while keeping async load execution in the macOS shell. No-model-selected recorder failure and diagnostics copy comes from `VoiceInkModelManagementPresentation`; the shared streaming-keys migration also repairs stored model names through the same preference, and legacy `"CurrentModel"` cleanup now lives with that shared preference.
- macOS `AIService`, `PowerModeConfig`, `PowerModeConfigView`, and `SystemInfoService` use `VoiceInkAIEnhancementProviderPreference` for selected AI provider/model storage, selected-model map loading, and diagnostics presentation; macOS still owns API-key lookup, Ollama/OpenRouter dynamic state, and provider execution.
- macOS `AIEnhancementService` delegates selected-text, clipboard, current-window, raw custom-vocabulary prompt terms, custom-vocabulary tag assembly, completed enhancement-result construction, and screen-capture context window selection/text formatting to `VoiceInkAIEnhancementPromptBuilder`/`VoiceInkAIEnhancementVocabularyContext`/`VoiceInkAIEnhancementScreenContext`/`VoiceInkAIEnhancementResult`; successful enhancement returns that shared result, so recorder/import/retry/re-enhance flows persist enhanced text, duration, model, prompt, and request messages from one shared metadata object while macOS still owns Accessibility, pasteboard, ScreenCaptureKit/Vision capture execution, provider execution, and SwiftData vocabulary collection.
- macOS recorded-file cloud transcription passes raw SwiftData vocabulary terms into shared remote batch options, and streaming adapters fetch use-specific normalized terms through `CustomVocabularyService`; `VoiceInkCustomVocabularyUse` owns provider/use-specific support and term-limit code.
- macOS prompt-trigger restoration uses `VoiceInkPromptDetectionResult` settings-state restoration planning, so temporary trigger-selected prompts restore the original selected prompt even when the original selection was nil.
- macOS recorder and audio-file transcription enhancement failures use `VoiceInkPostProcessingFailurePresentation`, colocated with transcript presentation, for stored failure text, notification title truncation, and disabled/not-configured re-enhance guard copy; audio-file import/retry success, failure, and not-attempted completion now run through shared completed-draft policy while preserving their current shell-specific failure text policy.
- macOS audio-file transcription retry, last-transcription retry, audio-player retranscribe/re-enhance status banners, and failure surfaces now use `VoiceInkTranscriptPresentation` plus the shared engine-error vocabulary in `VoiceInkErrorDescription`/`VoiceInkEngineError`, matching iOS record failure text and shared run-processor failures while keeping shell ownership of notifications, banners, and queue state.
- macOS audio-file import/retranscription uses `VoiceInkEngineError` for no-model-selected and missing-stored-audio failures, and no longer keeps unused shell-only observable transcription state.
- macOS audio-file import and retranscription records now persist the shared `.completed` transcription status explicitly; `VoiceInkTranscriptionStatus` lives with the shared transcription record mutation plans, matching the recorder pipeline and iOS shared record updater while keeping SwiftData model storage in the macOS shell.
- macOS audio-file import and retranscription use `VoiceInkPostProcessingSkipPolicy` from the transcription run preparation surface, matching the recorder pipeline and iOS run processor short-transcript skip behavior while preserving macOS prompt-trigger override on retry.
- macOS `TranscriptionAutoCleanupService`, `ImportExportService`, `BackupImporter`, `SystemInfoService`, and app startup read/write transcription auto-cleanup preferences through `VoiceInkTranscriptionAutoCleanupPreference`; backup import/export plans also live there, while `AudioCleanupSettingsView` uses `VoiceInkMacOSCleanupSettingsPresentation` for cleanup labels, retention options, and result copy while still binding directly through `@AppStorage`.
- macOS `AudioCleanupManager`, audio cleanup settings, backup import/export, app defaults, and system diagnostics read/write audio-file cleanup preferences through `VoiceInkAudioCleanupPreference`; backup import/export plans also live there, while `AudioCleanupSettingsView` uses `VoiceInkMacOSCleanupSettingsPresentation` for audio cleanup labels, action copy, and alerts and macOS still owns transcript queries, file deletion, cleanup timers, file-size formatting, and UI state.
- iOS retry post-processing and macOS AI enhancement inherit `VoiceInkAIReasoningConfig.chatRequestParameters` for OpenAI-compatible request temperature, reasoning effort, and extra body values, keeping provider execution adapters platform-owned.
- iOS retry post-processing failure text is produced through `VoiceInkPostProcessingFailurePresentation`, colocated with transcript presentation, keeping the existing `"Post-processing failed:"` prefix shared with core tests.

Current iOS consumers of shared remote transport:

- `iOS/VoiceInk-ios/TranscriptionRetryService.swift` gets retry transcription services through `VoiceInkAudioTranscriptionServiceFactory`, so shared core owns remote-vs-local provider dispatch while iOS supplies only its local Whisper adapter.
- `VoiceInkRemoteTranscriptionService` dispatches Groq/OpenAI/Cerebras, Deepgram, Gemini, Mistral, ElevenLabs, Soniox, Speechmatics, AssemblyAI, and xAI through shared core clients.
- Provider-initialized `VoiceInkRemoteTranscriptionService.transcribeAudioFile` now derives file transcription options from the same `VoiceInkRemoteTranscriptionOptions.batchDefaults` policy used by macOS batch cloud transcription, while direct transport initialization keeps prompt-only custom behavior.
- `VoiceInkAudioTranscriptionService` is transcription-only; API-key verification is kept in `VoiceInkProviderAPIKeyVerifier` so retry transcription adapters do not expose a dead verification interface.
- `iOS/VoiceInk-ios/TranscriptionRetryService.swift` routes iOS transcription through `VoiceInkTranscriptionRunProcessor`, including shared output filtering, provider transcription empty-output policy, cleanup preferences from `AppSettings`, a macOS-order word-replacement hook before post-processing, and shared mutable-record transitions for completed and failed retry results.
- iOS retry and background recording transcription now apply stored-audio retranscription success, missing-audio, and failure state through the `TranscriptionRetryService` adapter, which owns the shared `VoiceInkMutableTranscriptionRecord` + `VoiceInkStoredAudioRecord` default call and failure routing through `VoiceInkTranscriptionRecordFailurePlan`; `RecordingManager` receives stopped-recording pending rows and missing-audio cleanup behavior through `VoiceInkRecordingStopPlan`, leaving the iOS recording shell responsible only for starting background work, SwiftData saves, and recording cleanup execution.
- iOS note detail and list deletion consume `VoiceInkStoredAudioRecord`/`VoiceInkStoredAudioAvailability` for audio availability presentation/icon and audio-file deletion, `VoiceInkTranscriptPresentation.retryControls`/`statusErrorDetail` for pending-vs-failed retry panel state and error visibility, and `VoiceInkTranscriptPresentation.deletionTargets` for filtered-list delete target selection; iOS still owns SwiftUI layout, retry task execution, and SwiftData record deletion.
- `VoiceInkTranscriptionRunProcessor` now measures transcription and successful post-processing durations in shared core, and `VoiceInkMutableTranscriptionRecord.applyCompletedRunResult(_:)` persists them, so iOS retry/background transcription records feed the same metric and performance fields as macOS records.
- iOS `Transcription` records conform to both `VoiceInkSessionMetricSource` and `VoiceInkDashboardMetricRecord`, inheriting dashboard word/audio values from the shared session metric policy; `iOS/VoiceInk-ios/NotesListView.swift` builds its note-list word/audio, count display, fastest-model summary, header label, settings SF Symbol, and recording action label/icon through `VoiceInkNoteListSummaryPresentation`/`VoiceInkNoteListPresentation`, so the iOS shell consumes the same dashboard metric and model performance interfaces as macOS while only rendering the shared presentation.
- `VoiceInkNoteListSummaryPresentation` derives fastest-model note-list summary and speed-factor display text from `VoiceInkPerformanceAnalyzer`, so iOS consumes the same shared model performance interface as the macOS metrics panels while keeping the iOS UI shell separate.
- iOS note-list empty state uses `VoiceInkHistoryPresentation.iOSNotesEmptyState`, and recording-start alerts fall back to `VoiceInkRecordingSheetPresentation.iOS.cancelButtonTitle`, keeping app-specific layout in SwiftUI while sharing empty-state/action copy and SF Symbol selection with the history/recording presentation modules.
- `iOS/VoiceInk-ios/TranscriptionRetryService.swift` passes `VoiceInkPostProcessingSkipConfiguration.current()` into `VoiceInkTranscriptionRunProcessor`, so iOS retry transcription follows the same short-transcript post-processing skip policy from transcription run preparation as macOS unless a prompt trigger explicitly forces post-processing.
- `iOS/VoiceInk-ios/AppSettings.swift` and `VoiceInkTranscriptionRunProcessor` use the shared provider credential policy so whitespace-only provider keys fail as missing before provider transport runs.
- `iOS/VoiceInk-ios/AppSettings.swift` loads retry transcription cleanup configuration through `VoiceInkTranscriptionCleanupConfiguration.current()`, aligning iOS cleanup with macOS defaults while keeping settings UI/storage in the iOS shell.
- `iOS/VoiceInk-ios/AppSettings.swift` passes the shared paragraph-formatting preference into `VoiceInkTranscriptionRunProcessor`, so iOS retry transcription uses the same `VoiceInkTranscriptParagraphFormatter` policy as macOS.
- `iOS/VoiceInk-ios/TranscriptionRetryService.swift` passes the iOS selected transcription language through `VoiceInkTranscriptionRunProcessor`, which normalizes auto-detect before remote/local transcription adapters receive it.
- `iOS/VoiceInk-ios/WhisperTranscriptionService.swift` passes the shared selected-language local Whisper prompt helper into the iOS whisper.cpp wrapper and returns empty local transcripts unchanged, so iOS inherits the macOS language seed prompts and the shared local empty-output policy.
- `iOS/VoiceInk-ios/WhisperTranscriptionService.swift` resolves the selected mode's local Whisper model path through `VoiceInkWhisperModelFiles`, preserving today's base-model behavior while keeping future local model selection on the shared runtime-model contract.
- `iOS/VoiceInk-ios/LocalModelManager.swift` applies model-download install/failure/cancellation outcomes through `VoiceInkWhisperModelSimpleDownloadCompletionPlan`, downloaded-model deletion requests through `VoiceInkWhisperModelDeletionPolicy`, and lifecycle log text through `VoiceInkWhisperModelManagementDiagnostics`; iOS onboarding and local-model management render downloaded/download-progress state, management rows, row/action presentation, onboarding primary actions, row action affordances, operation confirmations, operation failure alerts, and delete-action diagnostic copy through `VoiceInkWhisperModelDownloadState`/`VoiceInkWhisperModelDownloadProgress`/`VoiceInkWhisperModelManagementList`/`VoiceInkWhisperModelDownloadRowPresentation`/`VoiceInkWhisperModelDownloadRowAction`/`VoiceInkOnboardingModelDownloadPrimaryAction`/`VoiceInkWhisperModelOperationConfirmationPresentation`/`VoiceInkWhisperModelOperationAlertPresentation`/`VoiceInkWhisperModelManagementDiagnostics`, so download/downloaded/cancel/delete affordance branching crosses shared action enums/plans instead of shallow row-action booleans while the iOS shell still owns selected model choice, URLSession task/progress state, OSLog delivery, and file-operation execution.
- `iOS/VoiceInk-ios/LibWhisper.swift` reads `VoiceInkWhisperRuntimeConfiguration` and applies it through the shared runtime parameter-sink Interface before calling whisper.cpp, and now uses `VoiceInkWhisperTranscriptSegments` from the local Whisper runtime policy for raw segment count/text lookup and concatenation instead of trimming or looping in the adapter. Cleanup still happens through `VoiceInkTranscriptionRunProcessor`, matching the shared macOS-order output pipeline while keeping iOS whisper.cpp execution in the iOS shell.
- `iOS/VoiceInk-ios/AudioRecorder.swift` maps AVFoundation recorder settings from `VoiceInkIOSAudioRecorderConfiguration` in the shared iOS audio configuration surface, applies stop/discard cleanup decisions from `VoiceInkAudioRecorderStopPolicy`, and throws `VoiceInkAudioRecorderStartFailurePolicy.returnedFalseError()` for the AVAudioRecorder start-false failure shape, so iOS live recording uses the same linear PCM, 16 kHz mono, 16-bit little-endian integer, high-quality, metered format contract as macOS local transcription while keeping AVAudioRecorder dictionary adaptation, capture/session lifecycle, timer invalidation, file deletion, and session deactivation execution in the iOS shell.
- macOS `Recorder` and iOS `AudioRecorder` apply raw meter decibels through `VoiceInkAudioMeterLevel` update plans and read platform meter update cadence there; macOS recorder visualizer geometry and animated bar-height math plus iOS visible level history, visualizer geometry constants, bar width/height math, and recent-sample visualizer bar mapping share the same audio-meter policy, while platform shells still own timer delivery, smoothing state storage, color, layout containers, and rendering.
- iOS `RecordingManager` uses `VoiceInkRecordingState`, shared active-recording predicate, `VoiceInkRecordingFlowState`, `VoiceInkRecordingPermissionPolicy`, `VoiceInkRecordingStopPlan`, `VoiceInkAudioRecorderStopPolicy`, `VoiceInkRecordingStartPolicy`, and `VoiceInkRecordingAlertPresentation` for start/stop/cancel/sheet/duration state transitions, microphone permission start/request/denied action planning, permission settings-open fallback planning, start-button mode-count gating, stopped-recording pending-draft creation, recorder stop/discard cleanup planning, missing-audio stop cleanup, no-mode, microphone-permission, microphone-busy, AVAudioRecorder start-false, and generic recording-failure alert content; `AudioRecorder` uses `VoiceInkAudioRecorderStartFailurePolicy` for the AVAudioRecorder start-false error domain/code/userInfo while preserving iOS-specific permission requests, settings URL/open execution, App Group coordination, audio-session behavior, Timer scheduling, AVFoundation execution, and SwiftData writes in the shell. iOS `VoiceInk_iosApp` routes keyboard deep-link recording requests through `VoiceInkLaunchRecordingRequestState`, so "start now vs defer until onboarding completes vs no-op" policy stays shared while SwiftUI scene selection and delayed dispatch stay in the shell. `iOS/Shared/VoiceInkAppGroupRecordingBridge.swift` adapts `VoiceInkAppGroupRecordingStatePolicy` to UserDefaults, and `AppGroupCoordinator.swift` adapts `VoiceInkAppGroupRecordingDiagnostics` to OSLog, so the app/keyboard stale-state timeout, storage keys, read plan, stale-state repair mutation plan, write plans, mutation-to-Darwin-notification plan, and App Group recording-state diagnostic copy stay shared while UserDefaults, Darwin notification execution, and OSLog delivery stay in the shell. `KeyboardViewController` routes record-button taps through `VoiceInkKeyboardRecordingButtonTapPolicy`, so stop-vs-open-app and post-stop refresh intent stay shared while UIKit owns haptics, App Group stop execution, URL opening, and colors. `iOS/Shared/VoiceInkKeyboardURLOpener.swift` consumes `VoiceInkKeyboardOpenAppPolicy` and `VoiceInkKeyboardOpenAppDiagnostics`, so extension-context-vs-application-vs-responder-chain fallback order and log copy stay shared while `extensionContext.open`, `UIApplication`, and responder-chain execution stay in the UIKit shell. The iOS app routes keyboard stop notifications through `VoiceInkKeyboardStopRecordingRequestPolicy`, so active-recording stop-vs-ignore handling stays shared while notification delivery and SwiftData stop execution stay in the shell. `RecordingSheetView` and retry status mode selection use `VoiceInkModeSelectionPresentation`/`VoiceInkRecordingSheetPresentation` through an iOS SwiftUI adapter, and iOS settings/configuration mode surfaces use `VoiceInkModeSummaryPresentation`, `VoiceInkModeFormPresentation`, and `VoiceInkModeFormProviderAvailability`, so picker-vs-single-label, sheet control copy, stop-button icon, mode-summary text, mode-form copy, and paired provider save/repair availability stay shared while rendering stays in the shell.
- `iOS/VoiceInk-ios/Transcription.swift` supplies its Documents/Recordings directory through `VoiceInkStoredAudioRecord`, so iOS note detail, retry transcription, and deletion share the same record-level audio-file behavior as macOS.
- `iOS/VoiceInk-ios/ProviderAPIKeyView.swift` verifies stored provider keys through `VoiceInkProviderAPIKeyVerifier` and delegates draft-key verification/save-after-verify policy, verification start planning, verification side-effect application, form editing state transitions, verified stored-key row presentation, form presentation copy/action icons, and verification-result presentation/icons to `VoiceInkProviderAPIKeyDraft`/`VoiceInkProviderAPIKeyFormState`/`VoiceInkProviderAPIKeyVerificationStartPlan`/`VoiceInkProviderAPIKeyFormPresentation`/`VoiceInkProviderAPIKeyVerificationProgress`; `iOS/VoiceInk-ios/AppSettings.swift` delegates provider-key state loading, stored/runtime key state, readiness, list rows, stored-key mutation plans, verification-flag mutation plans, verification reset-on-change behavior, mode provider availability lists, and API-key list row status to `VoiceInkProviderAPIKeyState`. iOS and macOS SwiftUI API-key tone colors each live in one shell adapter, while macOS provider-key screens use shared form state, draft-key policy, verification start/application planning, and verification progress presentation for submit enablement/result text, and `APIKeyManagementView` uses `VoiceInkAIEnhancementAPIKeyFormState` for draft construction, verification progress, key clearing, and failure alert copy, so shared core owns blank-key policy, environment-reference save/runtime application, stored-key obfuscation selection, and feedback copy before transport verification without changing platform key-storage behavior.
- iOS note-list, onboarding title copy, keyboard fallback title, and the iOS launch UI smoke test read `VoiceInkAppIdentity` plus `VoiceInkIOSOnboardingPresentation`; iOS app, keyboard, App Group, audio-session, recording, notes, settings, audio-playback, local-Whisper service, and local-model-management diagnostics route through `VoiceInkIOSLogger` backed by `VoiceInkAppIdentity.loggingSubsystem` and `VoiceInkIOSLogCategory`; macOS/iOS storage startup failure copy routes through `VoiceInkStorageStartupDiagnostics`; macOS/iOS whisper.cpp adapters use the same logging subsystem and shared runtime diagnostics category; macOS SwiftData CloudKit uses its container identifier; iOS App Group adapters use its shared iOS bridge identifiers; iOS app/keyboard record URL handling uses `VoiceInkAppDeepLink`; and iOS recorder errors use its bundle-scoped error domain helper, so app identity presentation and diagnostics stay aligned with macOS.
- `iOS/VoiceInk-ios/AppSettings.swift`, `SettingsView.swift`, and `ModeConfigurationView.swift` delegate active-mode transcription language availability, provider-change model repair, combined selected-mode/selected-language repair plus write decisions, mode-list deletion, prompt-template draft state, paired provider availability, mode saveability, post-processing/custom-prompt form visibility, save-button availability, top-level settings chrome/action copy, provider model fixed-vs-picker presentation, and mode-form presentation copy to shared core policy; `SettingsView` keeps only the SwiftUI option rendering and platform reset execution.
- `VoiceInkTranscriptionRunProcessor` callers receive runtime mode configurations with stale transcription and post-processing model IDs repaired by `Mode`, so persisted iOS mode records cannot bypass the shared provider model-selection policy.
- `VoiceInkModeStorage.loadModes()` repairs stale transcription and post-processing model IDs before `AppSettings` exposes loaded modes to iOS UI or retry transcription.
- `iOS/VoiceInk-ios/TranscriptionRetryService.swift` passes the selected transcription language and local Whisper prompt into `VoiceInkTranscriptionRunProcessor`, so runtime transcription input is explicit and the iOS local Whisper adapter does not read app preferences directly.
- `iOS/VoiceInk-ios/TranscriptionRetryService.swift` gets retry transcription services through `VoiceInkAudioTranscriptionServiceFactory`, so shared core owns remote-vs-local provider dispatch while iOS supplies only its local Whisper adapter.
- `iOS/VoiceInk-ios/WhisperTranscriptionService.swift` throws `VoiceInkEngineError` selected by `VoiceInkLocalWhisperFailurePolicy` for local Whisper model, audio-processing, and transcription failures instead of owning iOS-only local Whisper error vocabulary.
- Cartesia remains absent from iOS transcription provider selection until an iOS streaming adapter exists; it is not a batch provider.
- The bundled `VoiceInk` provider case remains decodable and ready as a bundled service, but shared provider selectability keeps it hidden from iOS transcription and post-processing selection until a real no-key/bundled-service adapter exists. The sibling clone marked it always available while returning an empty API key, so porting that path would preserve a broken no-key mode.

Platform shells still own UI, OS permissions, audio capture, paste/keyboard behavior, local model download storage, SwiftData models, and macOS-only orchestration. macOS keeps its Keychain adapter for local-build fallback/logging; iOS calls the shared Keychain data store directly. iOS-only shell code shared between the app and keyboard extension lives in `iOS/Shared/`; this currently includes the App Group defaults adapter, Darwin notification posting/observing, keyboard URL-opening adapters, and the `VoiceInkIOSLogger` diagnostics adapter backed by `VoiceInkAppIdentity.loggingSubsystem` plus `VoiceInkIOSLogCategory`. The App Group recording-state keys, stale-recording expiry policy, stale-state repair mutation plan, write plans, mutation-to-Darwin-notification plan, App Group recording-state diagnostic copy, record deep-link contract, keyboard recording timing, keyboard button presentation, keyboard open-main-app fallback order/diagnostic copy, app-local keyboard stop notification name, and shared logging identity now live in `VoiceInkCore`.

iOS app-local storage roots are provided by `VoiceInkIOSStorageDirectories` in `VoiceInkCore`: shared core owns the Documents/Caches lookup, recordings/model subdirectory derivation, and best-effort preparation through `VoiceInkStoredAudioFile` and `VoiceInkWhisperModelFiles`; iOS shell callers only consume the shared URLs.
The migration verifier fails if new iOS app/keyboard/shared shell files recreate Documents/Caches roots or `Recordings`/`WhisperModels` path literals outside shared core, and it requires the old iOS storage wrapper file to stay deleted.
macOS app-local storage roots are provided by `VoiceInkMacOSStorageDirectories` in `VoiceInkCore`: shared core owns the Application Support lookup, app-support subdirectory, recordings/model subdirectory names, and custom-sound relative directory policy. The macOS shell callers only consume the shared URLs, and the migration verifier fails if the old macOS storage wrapper file returns or if macOS app code rebuilds those roots outside shared core.

## Sibling Clone Status

The remaining Swift files present in `../VoiceInk-iOS/VoiceInk-ios` but not in `VoiceInk/iOS/VoiceInk-ios` are old clone-side sources, except where noted. `scripts/verify-ios-single-repo-migration.sh` now fails if any of these obsolete app-target duplicates are copied back under `iOS/VoiceInk-ios/` or named by `iOS/VoiceInk-ios.xcodeproj/project.pbxproj`; the sibling keyboard, unit-test, and UI-test target folders must have no sibling-only Swift files except the documented obsolete UI-test launch screenshot template.

- `AppGroupCoordinator.swift`: moved into `iOS/Shared/AppGroupCoordinator.swift`; start-recording requests now use `VoiceInkAppDeepLink` from `VoiceInkCore`, while `VoiceInkAppGroupRecordingStatePolicy` supplies App Group recording state decisions, stale-state repair mutation plans, mutation notification names, and diagnostic copy, and the iOS shared shell still handles stop requests, UserDefaults access, URL opening, OSLog delivery, and keyboard recording-state notification delivery.

- `DeepgramTranscriptionService.swift`: replaced by `VoiceInkCore` Deepgram request/client helpers and `VoiceInkRemoteTranscriptionService`.
- `GroqTranscriptionService.swift`: replaced by `VoiceInkCore` OpenAI-compatible request/client helpers and `VoiceInkRemoteTranscriptionService`.
- `OpenAICompatibleClient.swift`: replaced by `VoiceInkCore` chat DTOs/request builders, models-verification request builder, and client.
- `LLMPostProcessor.swift`: replaced by `VoiceInkPostProcessingClient`, its colocated `VoiceInkPostProcessingRequest`, and `VoiceInkTranscriptionRunProcessor`.
- `TranscriptionServiceFactory.swift`: replaced by `VoiceInkTranscriptionRunProcessor` service-provider injection plus the iOS local Whisper adapter and shared `VoiceInkRemoteTranscriptionService`.
- `KeychainService.swift`: replaced by iOS `AppSettings` calls into `VoiceInkProviderAPIKeyStorage`; macOS keeps its adapter only for local-build fallback storage and logging.
- `RiffWaveUtils.swift`: replaced by `VoiceInkPCM16AudioSamples`.
- `VADModelManager.swift`: replaced by direct `VoiceInkVADModelFiles.sileroPath()` calls from the macOS/iOS Whisper shells and macOS rolling preload; the helper now lives with `VoiceInkWhisperModelFiles`.
- `DefaultModeManager.swift`: replaced by `VoiceInkIOSFirstTimeSetupPolicy` plus `VoiceInkModeSettingsPolicy.defaultModeRepairPlan`.
- `Mode.swift`, `PromptTemplate.swift`, `Provider.swift`: replaced by `VoiceInkCore` mode and provider catalog modules; post-processing template type/copy is colocated with `VoiceInkPostProcessingPromptTemplate` in the shared mode policy.
- `ModeSelectionView.swift`, `ModesView.swift`: obsolete iOS UI experiments; current in-repo iOS mode UI is `iOS/VoiceInk-ios/ModeConfigurationView.swift`.
- `ContentView.swift`: obsolete clone app-root wrapper; current in-repo iOS launch path presents `NotesListView` directly after onboarding.
- `Item.swift`: unused SwiftData template sample.
- `VoiceInk-ios/Transcription.swift`: obsolete nested clone-side model copy; current in-repo iOS model is `iOS/VoiceInk-ios/Transcription.swift`.
- In-repo-only iOS app Swift files are limited to `ProviderAPIKeyTone+iOS.swift`, `TranscriptStatusTone+iOS.swift`, and `Transcription.swift`; these are the intentional iOS shell adapters/model file after the single-repo import, not sibling clone drift.
- `VoiceInk_iosTests.swift` and `VoiceInk_iosUITests.swift`: kept in the in-repo iOS target as real migration/runtime smoke coverage; pure `VoiceInkCore` contract assertions stay in `VoiceInkCoreTests`, and the old stock-template assertions from the sibling clone should not be copied back.
- `VoiceInk_iosUITestsLaunchTests.swift`: obsolete stock launch-screenshot template; the real launch smoke assertion lives in `VoiceInk_iosUITests.swift`, so this file stays absent from the in-repo iOS UI-test target.

Do not copy these files back into `VoiceInk/iOS`. If behavior from one appears missing, port it into `VoiceInkCore` or the appropriate platform shell with a focused test.

## Deletion Gates

Before treating `../VoiceInk-iOS` as obsolete, verify current state from `VoiceInk/`:

Run the bundled static verifier first:

```bash
scripts/verify-ios-single-repo-migration.sh
```

When real Xcode, app dependencies, and the iOS platform are installed, run the same verifier with app builds enabled:

```bash
scripts/verify-ios-single-repo-migration.sh --full-build
```

1. `VoiceInk.xcworkspace` includes `iOS/VoiceInk-ios.xcodeproj`, the shared iOS scheme includes the unit-test and UI-test bundles, and Xcode metadata does not reference the sibling `VoiceInk-iOS` clone.
2. macOS and iOS projects both resolve `VoiceInkCore` from inside `VoiceInk/`, and the iOS app target links the `VoiceInkCore` product.
   - Local Whisper framework dependency: both app projects reference `$(HOME)/VoiceInk-Dependencies/whisper.cpp/build-apple/whisper.xcframework`, and clone-side `../Downloads`, `../whisper.cpp`, and `../build-apple` framework references stay absent.
3. `VoiceInk/` is the git root for this work, and the abandoned parent-level Swift package shape (`../VoiceInkCore`, `../Package.swift`, `../Sources/VoiceInkCore`, `../Tests/VoiceInkCoreTests`) remains absent.
4. `VoiceInkCore` stays platform-neutral: no AppKit, UIKit, SwiftUI, SwiftData, AVFoundation, CoreAudio, IOKit, FluidAudio, KeyboardKit, LLMkit, WhisperKit, or whisper.cpp module imports in shared sources or core checks.
5. iOS App Group recording-state keys, stale-state policy, App Group diagnostic copy, record deep-link contract, keyboard recording timing, deferred app-launch recording request policy, keyboard button presentation, keyboard open-main-app fallback order/diagnostic copy, and app-local keyboard stop notification name route through `VoiceInkCore`, while Darwin notification execution, timers, UIKit rendering, OSLog delivery, and keyboard URL-opening execution stay out of `VoiceInkCore`.
6. macOS and iOS app product/display names stay `roma just talk`, the iOS keyboard extension display name stays `roma just talk`, and the iOS record deep-link scheme stays `voiceink`.
7. The iOS app and keyboard extension entitlements use the same App Group identifier as `VoiceInkAppGroupRecordingBridge`.
8. `swift run VoiceInkCoreChecks` passes from `VoiceInkCore/`.
9. macOS Swift sources parse or build.
10. iOS app, keyboard, unit-test, and UI-test Swift sources parse or build.
11. `plutil -lint` passes for both project files and iOS plists/entitlements.
12. `xmllint --noout` passes for workspace and shared scheme XML.
13. iOS shared shell files are present under `iOS/Shared/` and included in both the iOS app and keyboard targets: `AppGroupCoordinator.swift`, `VoiceInkAppGroupRecordingBridge.swift`, `VoiceInkIOSLogger.swift`, and `VoiceInkKeyboardURLOpener.swift`; `VoiceInkAppGroupRecordingBridge` adapts the shared App Group recording-state policy to UserDefaults, `AppGroupCoordinator` adapts shared App Group diagnostics to OSLog, `VoiceInkIOSLogger` adapts the shared app logging identity and shared iOS log categories to OSLog, `VoiceInkKeyboardURLOpener` adapts the shared open-app policy/diagnostics to the keyboard extension's record deep-link `extensionContext.open`, `UIApplication`, and responder-chain fallbacks, and notification execution stays there instead of app-only managers. Deferred app-launch recording requests stay in `VoiceInkLaunchRecordingRequestState` instead of a loose app-level pending flag. The old `iOS/Shared/VoiceInkAppDeepLink.swift`, `iOS/Shared/VoiceInkKeyboardRecordingTiming.swift`, and `iOS/Shared/VoiceInkKeyboardRecordingButtonPresentation.swift` wrappers stay deleted because those contracts live in `VoiceInkCore`.
14. iOS non-Swift app artifacts are present: privacy manifest, app icon catalog files, and bundled Silero VAD resource.
15. Obsolete clone-side Swift duplicates remain absent from `iOS/VoiceInk-ios/` and from the iOS project file.
16. When `../VoiceInk-iOS` exists, every sibling-only Swift file under `VoiceInk-ios/` is one of the documented obsolete/replaced files above, every in-repo-only iOS app Swift file is one of the documented intentional shell/model files above, and the sibling keyboard/unit-test/UI-test target folders have no sibling-only Swift files except the obsolete UI-test launch screenshot template.
17. Legacy same-file clone shims remain absent from the in-repo iOS app and keyboard: `TranscriptionServiceFactory`, `LLMPostProcessor`, the old local `RecordingState`, raw `voiceink://record` URL construction, shell-owned keyboard button copy, `Open VoiceInk` keyboard copy, and old `AppSettings` effective-provider/model accessors.
18. Standalone clone setup/Pages files such as `iOS/.github/workflows/deploy.yml`, `iOS/.nojekyll`, and `iOS/tasks.md` remain absent from the in-repo iOS subtree.
19. iOS recording background transcription delegates stored-audio completion/failure updates to `TranscriptionRetryService.retranscribe(note:)`; the retry adapter owns the `VoiceInkMutableTranscriptionRecord.retranscribeStoredAudio` call instead of exposing that record-mutation seam to `RecordingManager`.
20. macOS recorder failure state routes through `VoiceInkTranscriptionRecordFailurePlan`, and canceled record mutation routes through `VoiceInkTranscriptionRecordCancellationPlan`, instead of duplicating status/text/metadata-clearing decisions in the pipeline or SwiftData shell.
21. macOS live recording builds pending rows through `VoiceInkRecordingTranscriptionDraft.pending`, now colocated with completed transcription draft construction; iOS live recording receives pending rows and missing-audio cleanup behavior from `VoiceInkRecordingStopPlan`; macOS canceled recordings build through `VoiceInkRecordingTranscriptionDraft.canceled`.
22. iOS live recording delegates timestamped filename construction, PCM16 recorder format constants, recorder configuration policy, recorder stop/discard cleanup decisions, and AVAudioRecorder start-false error construction to `VoiceInkStoredAudioFile`, `VoiceInkPCM16Audio`, `VoiceInkIOSAudioRecorderConfiguration`, `VoiceInkAudioRecorderStopPolicy`, and `VoiceInkAudioRecorderStartFailurePolicy`.
23. iOS note detail and list deletion route stored-audio availability, unavailable icon, deletion, failed-delete log copy, retry panel state, status error visibility, and filtered-list delete target selection through `VoiceInkStoredAudioRecord`/`VoiceInkStoredAudioAvailability`/`VoiceInkStoredAudioFile`/`VoiceInkTranscriptPresentation`; macOS common transcript copy/save button copy/icons and export file extensions route through `VoiceInkTranscriptPresentation`/`VoiceInkTranscriptFileExport`.
24. macOS recording cancellation routes through `VoiceInkMacOSRecordingCancellationPolicy`; macOS and iOS microphone permission start/request/denied action planning plus iOS permission settings-open fallback planning route through `VoiceInkRecordingPermissionPolicy`; macOS recorder UI active-shortcut and stale hidden-recorder session policy routes through `VoiceInkRecorderUISessionPolicy`; and iOS recording flow state, start-button mode-count gating, keyboard record-button tap action planning, app-side keyboard stop-request handling, stopped-recording pending-draft/missing-audio cleanup planning, recorder stop/discard cleanup planning, recording/retry mode selection, recording sheet controls/icon, settings mode list mutation/default repair, mode summaries, mode-configuration form copy, and paired provider availability route through `VoiceInkRecordingFlowState`, `VoiceInkRecordingStartPolicy`, `VoiceInkKeyboardRecordingButtonTapPolicy`, `VoiceInkKeyboardStopRecordingRequestPolicy`, `VoiceInkRecordingStopPlan`, `VoiceInkAudioRecorderStopPolicy`, `VoiceInkModeSelectionPresentation`, `VoiceInkRecordingSheetPresentation`, `VoiceInkModeListPolicy`, `VoiceInkModeSummaryPresentation`, `VoiceInkModeFormPresentation`, and `VoiceInkModeFormProviderAvailability`.
25. iOS onboarding/local model row assembly, row status, model-download action icons/affordances, onboarding primary actions, swipe delete action text, download completion install/failure/cancellation decisions, downloaded-model delete request planning, operation confirmations, and failure alerts route through `VoiceInkWhisperModelManagementList`, `VoiceInkWhisperModelDownloadRowPresentation`, `VoiceInkWhisperModelDownloadRowAction`, `VoiceInkOnboardingModelDownloadPrimaryAction`, `VoiceInkWhisperModelSimpleDownloadCompletionPlan`, `VoiceInkWhisperModelDeletionPolicy`, `VoiceInkWhisperModelOperationConfirmationPresentation`, and `VoiceInkWhisperModelOperationAlertPresentation`; local-model views switch on the shared row/onboarding action instead of consuming shallow `isDownloaded`/`canStartDownload`/`canCancelDownload`/`canDeleteDownloadedModel` predicates, while macOS downloaded-local-model lookup plus downloaded/imported-local-model record construction routes through `VoiceInkWhisperModelFiles`, leaving transfer execution and notifications in the macOS shell.
26. macOS audio-file transcription queue status/phase policy, queue mutation policy, cancel-reset policy, queue count text, drop-target copy, action labels/help, and row status/action icons route through `VoiceInkAudioFileQueueStatus`/`VoiceInkAudioFileQueuePolicy`/`VoiceInkAudioImportPresentation`/`VoiceInkAudioFileQueuePresentation`, with open panels, drag/drop, queue storage mutation, SwiftData insertion, and transcription execution remaining in the macOS shell.
27. iOS API-key form copy/action icons, stored-key edit/reset planning, save/verify control presentation, verification-result feedback visibility, and list status route through `VoiceInkProviderAPIKeyFormState`, `VoiceInkProviderAPIKeyFormPresentation`, and `VoiceInkProviderAPIKeyListRowPresentation`; iOS tone colors for provider-key rows and verification feedback live in one shell adapter.
28. macOS Keychain adapter and iOS API-key settings route shared generic-password add/copy/delete/exists query, `SecItem*` execution policy, and UTF-8 string value storage policy through `VoiceInkKeychainQuery`/`VoiceInkKeychainDataStore`/`VoiceInkKeychainValueStore`, with duplicate service/syncability/data-protection query constants, direct `SecItem*` calls, and shell-owned API-key string encode/decode out of platform shells; the old iOS `KeychainService.swift` pass-through stays deleted.
29. iOS provider API-key storage routes provider account selection, skipped local/bundled providers, default empty loads, save/delete keychain calls, and save-failure status detection through `VoiceInkProviderAPIKeyStorage`, with direct provider-account and `VoiceInkKeychainValueStore` calls out of `AppSettings`.
30. macOS and iOS local Whisper services, model managers, and wrappers consume shared app logging identity, runtime diagnostics, runtime parameter assignment, context initialization, VAD resource, local model URL resolution, and PCM16 WAV sample policies; macOS FluidAudio model runtime consumes shared default-model, runtime version, and language-hint policy; macOS FluidAudio batch/streaming adapters consume shared local ASR pass scheduling, seek, cached-final, trailing-silence padding policy, and download status presentation; macOS cloud streaming adapters consume shared provider streaming connection model-name policy.
31. macOS AI-enhancement diagnostics/context toggles, macOS onboarding welcome presentation, macOS reset-onboarding settings alert presentation, iOS app-settings startup state, iOS audio-session recording configuration/timeout presentation/lifecycle state, first-run onboarding storage/presentation checks, startup legacy-preference migration sets, and iOS app-data reset file cleanup stay routed through shared `VoiceInkCore` preference/reset Modules.
32. macOS metrics setup step order, icons, labels, help text, and action-title policy route through `VoiceInkMacOSSetupPresentation`, with permission checks, navigation, and SwiftUI rendering remaining in the macOS shell.
33. macOS and iOS transcription-cleanup settings labels route through `VoiceInkTranscriptionCleanupPresentation`, macOS auto-cleanup settings labels/actions route through `VoiceInkMacOSCleanupSettingsPresentation`, and filler-word draft entry/submission stays routed through `VoiceInkFillerWordDraftState`/`VoiceInkFillerWordDraftSubmission`, with duplicate insert-plan unpacking, draft reset branching, and duplicate copy out of platform views.
34. iOS dictionary settings plus macOS dictionary form/chrome, quick-add, word-replacement guidance, edit-sheet, list/row copy, vocabulary draft text/submit/reset/duplicate-alert planning, word-replacement draft text/visibility/submit/reset/duplicate-alert planning, word-replacement edit state/saveability/update-completion planning, accepted-submission list application, sort mode storage/toggle/indicator policy, vocabulary/word-replacement list sorting, and sorted-row deletion planning route through `VoiceInkDictionarySettingsPresentation`/`VoiceInkDictionaryQuickAddPresentation`/`VoiceInkWordReplacementInfoPresentation`/`VoiceInkWordReplacementEditPresentation`/`VoiceInkVocabularyListPresentation`/`VoiceInkWordReplacementListPresentation`/`VoiceInkVocabularyDraftState`/`VoiceInkVocabularyDraftSubmission`/`VoiceInkVocabularySubmissionPlan`/`VoiceInkWordReplacementDraftState`/`VoiceInkWordReplacementDraftSubmission`/`VoiceInkWordReplacementSubmissionPlan`/`VoiceInkWordReplacementEditState`/`VoiceInkWordReplacementEditSubmission`/`VoiceInkDictionaryListSortPreference`/`VoiceInkDictionaryListSortPolicy`, with duplicate labels/placeholders/help text, local sort enums/raw keys, iOS word-replacement row arrow literals, obsolete submit wrappers, and shell-owned vocabulary/word-replacement insert-plan or displayed-row deletion branching out of platform views, quick-add, edit sheet, and iOS settings.
35. macOS Native Apple language-display fallback stays routed through `VoiceInkLanguageCatalog.nativeAppleDisplayName`, Native Apple unsupported-OS selection/runtime copy and result-stream timeout math stay routed through `VoiceInkNativeAppleTranscriptionPolicy`, macOS/iOS language setting copy stays routed through `VoiceInkTranscriptionLanguagePresentation`, and Native Apple language asset-control help/accessibility/icon presentation stays routed through `VoiceInkNativeAppleLanguageAssetPresentation`.
36. iOS note-list dashboard, count display, fastest-model summaries, header label, settings SF Symbol, recording action label/icon, and macOS dashboard hero/empty/action/stat-card/promotion/help-resource presentation stay routed through `VoiceInkNoteListSummaryPresentation`/`VoiceInkNoteListPresentation`/`VoiceInkDashboardPresentation`/`VoiceInkDashboardPromotionPresentation`/`VoiceInkHelpResourcesPresentation`; promotion visibility consumes `VoiceInkLicenseState` directly instead of a dashboard-only duplicate enum.
37. macOS and iOS date/duration-dependent UI plus metadata separator text stays routed through `VoiceInkDatePresentation`/`VoiceInkDurationPresentation`, colocated in the temporal presentation policy.
38. macOS and iOS audio playback state, timer-tick application, playback-rate controls, iOS playback session category policy, loading/play-pause/metadata-icon presentation, iOS playback failure diagnostic copy, macOS waveform/load failure console copy, macOS audio-player and inline-history audio-details action help copy, and macOS retranscribe/re-enhance action banner presentation stay routed through `VoiceInkAudioPlaybackState`/`VoiceInkAudioPlaybackTimerTickPlan`/`VoiceInkAudioPlaybackRate`/`VoiceInkIOSAudioPlaybackSessionConfiguration`/`VoiceInkAudioPlaybackPresentation`/`VoiceInkAudioPlaybackDiagnostics`/`VoiceInkAudioPlaybackActionBannerPresentation`.
39. macOS sidebar/window titles and iOS note-list/onboarding app-name copy stay routed through `VoiceInkAppIdentity`; macOS mini-recorder AppIntent title/description/dialog copy stays routed through `VoiceInkMiniRecorderAppIntentPresentation`, and mini-recorder request notification names stay routed through `VoiceInkMiniRecorderRequest`, both colocated with the app identity/event request module; iOS app/keyboard shell diagnostics stay routed through `VoiceInkIOSLogger` backed by `VoiceInkAppIdentity.loggingSubsystem` and `VoiceInkIOSLogCategory` instead of clone `print` calls or shell-owned category literals; macOS menu-bar icon visibility, dock-icon hiding storage/defaults, menu labels, empty-state copy, dynamic selection labels, and picker/checkmark symbols stay routed through `VoiceInkMenuBarPreference`/`VoiceInkMacOSMenuBarPresentation`; macOS main-window sidebar item labels/icons/visibility and notification-to-view selection stay routed through `VoiceInkMacOSMainViewItem`; macOS main-window navigation destinations plus file-transcription notification payload names stay routed through `VoiceInkMacOSNavigationRequest`/`VoiceInkMacOSFileTranscriptionRequest`.
40. macOS app logger subsystems, diagnostic subsystem filters, diagnostic log session storage/range/header/filename/error policy, diagnostic settings copy/icons, queue labels, and window identifiers stay routed through `VoiceInkAppIdentity.loggingSubsystem`/`VoiceInkDiagnosticLogExportPolicy`/`VoiceInkDiagnosticsSettingsPresentation`.
41. macOS history and iOS notes empty-state copy plus macOS history shortcut-tip copy/icon, search/pagination/selection copy, console diagnostic copy, and SF Symbol names stay routed through `VoiceInkHistoryPresentation`/`VoiceInkHistoryDiagnostics`; macOS full/inline history SwiftData query construction stays routed through `TranscriptionHistoryQuery`, and macOS full/inline history pagination, selection state math, selected-delete targets, post-delete focus/selection repair, and search/latest-record refresh decisions stay routed through `VoiceInkHistoryPaginationPolicy`/`VoiceInkHistorySelectionPolicy`/`VoiceInkHistoryDeletionPolicy`/`VoiceInkHistoryRefreshPolicy`, with platform views only rendering the shared presentation and executing SwiftData fetches/audio-file deletes/record deletes/reload tasks.
42. macOS transcription detail metadata labels/icons, AI request section labels, nil-only section visibility, and copy-button payload composition stay routed through `VoiceInkTranscriptionMetadataPresentation`, with `TranscriptionInfoPanel` only rendering the shared presentation.
43. macOS and iOS model-management labels route through `VoiceInkModelManagementFilter`, `VoiceInkModelManagementPresentation`, and `VoiceInkTranscriptionModelProviderRole`, including model language labels, provider role category/route/availability/streaming policy, AssemblyAI realtime/batch language selection, and imported-local-model copy, with duplicate model filter/default/local/cloud/custom/card action/badge/delete-confirmation copy and provider-role switches out of platform views and macOS model adapters.
44. macOS custom cloud model setup routes form defaults, labels, placeholders, action icons, default model description, and Keychain failure copy through `VoiceInkCustomCloudModelFormPresentation`; custom cloud model persistence routes the raw `customCloudModels` key plus JSON load/save through `VoiceInkCustomCloudModelStorage`; and custom OpenAI-compatible batch transcription routes request defaults, invalid-endpoint copy, empty-result acceptance, and no-plain-text-fallback decoding through `VoiceInkCustomCloudTranscriptionPolicy`, with HTTP API error classification through `VoiceInkCloudTranscriptionError`; SwiftUI state, Keychain writes, endpoint execution, and error presentation remain in the macOS shell.
45. macOS custom prompt icon catalog, prompt-card trigger summary/action symbols, grid empty/help copy and info icon, editor labels/placeholders/help/action symbols, and delete confirmation copy route through `VoiceInkCustomPromptPresentation`, with SwiftUI layout, popovers, drag/drop, and alert execution remaining in the macOS shell.
46. macOS cloud-provider and shared remote transcription API error-domain mapping stays routed through `VoiceInkTranscriptionModelProvider.apiErrorDomain`; the colocated remote transport helpers route shared HTTP response validation, retryable status-code classification, direct request execution, OpenAI-compatible transcription retry execution, validation, and long-running provider polling mechanics through `VoiceInkRemoteHTTPResponsePolicy`, `VoiceInkRetriedRequest`, and `VoiceInkRemotePollingPolicy`; API-key verification result handling stays routed through `VoiceInkAPIKeyVerificationPolicy`, with duplicate provider-domain switches out of the macOS shell and provider literals/error-response/verification-result/retry-loop/poll-loop validation out of `AudioTranscriptionService` plus low-level remote clients.
47. iOS debug reset sequencing, file cleanup, and SwiftData reset failure diagnostic copy route through `VoiceInkAppDataResetPlan`/`VoiceInkAppDataResetFilePlan`/`VoiceInkAppDataResetDiagnostics`, with raw SwiftData-records/files/settings ordering and recordings/models/cache/tmp deletion sequence out of `SettingsView`.
48. iOS onboarding step order, copy, feature rows, app-icon source/fallback decision, model-download step copy, ready step copy, button titles, and first-time setup default-mode/onboarding-completion planning route through `VoiceInkIOSOnboardingStep`, `VoiceInkIOSOnboardingPresentation`, `VoiceInkIOSAppIconPolicy`, and `VoiceInkIOSFirstTimeSetupPolicy`, with duplicate copy, raw integer step flow, local `lastIcon` fallback branching, and shell-owned setup sequencing out of `OnboardingView`/`AppSettings`.
49. iOS settings navigation title, modes/debug section titles, add-mode copy/icon, add-row icon, and reset-all-app-data copy/icon route through `VoiceInkSettingsPresentation`, with duplicate settings chrome/action copy out of `SettingsView`.
50. macOS announcements enablement, feed configuration, dismissed-id storage/trimming, active-window filtering, next-announcement presentation, action copy, close icon, and description visibility route through `VoiceInkAnnouncementPreference`/`VoiceInkAnnouncementPolicy`, with `Timer`, `URLSession`, AppKit panels/links, and SwiftUI rendering remaining in the macOS shell.
51. macOS final paste text assembly routes cursor-context capitalization planning, lowercase-cleanup preference gating, trial-expired prefix, trailing-space storage/default registration, and trailing-space settings labels/help through `VoiceInkTranscriptionPasteOutputPolicy`/`VoiceInkAppendTrailingSpacePreference`; `CursorPaster` adapts the shared cursor plan to macOS cursor lookup, with cursor paste, clipboard context lookup, sound feedback, auto-send timing, and settings rendering remaining in the macOS shell.
52. macOS paste method and clipboard restore settings route through `VoiceInkPasteMethod`/`VoiceInkPastePreference`, including settings labels/options/help and backup import/export plans, with AppKit pasteboard, AppleScript/CGEvent execution, and SwiftUI rendering remaining in the macOS shell.
53. macOS recording feedback preferences route through `VoiceInkSystemMuteMode`/`VoiceInkRecordingFeedbackPreference`, including experimental-feature backup import/export, pause-media fallback, recording/pause-media settings labels/options/help, and backup import/export plans. Custom recording sound catalog, storage keys/defaults, notification name, custom-sound URL and copy planning, duration limit, validation error copy, settings/menu/open-panel copy and icons, playback slots, volumes, and custom/default fallback order route through `VoiceInkBuiltInRecordingSound`/`VoiceInkCustomSoundType`/`VoiceInkCustomSoundPreference`/`VoiceInkCustomSoundSettingsPresentation`/`VoiceInkRecordingSoundPlaybackPolicy`, with CoreAudio, MediaRemote, bundle sound lookup, custom sound file IO execution, AVFoundation validation/playback, file-picker execution, and SwiftUI rendering remaining in the macOS shell.
54. macOS top-level Power Mode visibility, persist-after-recording preference, first-run visibility repair, and shortcut eligibility route through `VoiceInkPowerModePreference`, with settings rendering, shortcut monitoring, and session execution remaining in the macOS shell.
55. macOS VAD, model prewarm, and recorder transcript-preview preferences route through `VoiceInkVADPreference`/`VoiceInkModelRuntimePreference`/`VoiceInkRecorderPreviewPreference` plus `VoiceInkMacOSAdvancedTranscriptionSettingsPresentation`, with wake/app-launch scheduling, model-manager checks, and SwiftUI settings/recorder rendering remaining in the macOS shell.
56. macOS recording shortcut action identifiers, action display names, validation and monitor notification copy, mini-recorder escape confirmation copy, selection/mode migration plans, legacy shortcut key names, raw shortcut storage, middle-click, special empty-tap preferences, settings labels/help, shortcut recorder labels, backup import/export value planning, general-backup shortcut action ordering, and recording-shortcut selection repair when importing backed-up shortcut records route through `VoiceInkShortcutActionIdentifier`/`VoiceInkShortcutActionPresentation`/`VoiceInkShortcutValidationPresentation`/`VoiceInkMacOSShortcutNotificationPresentation`/`VoiceInkLegacyRecordingShortcutPreset`/`VoiceInkRecordingShortcutSelection`/`VoiceInkRecordingShortcutMode`/`VoiceInkRecordingShortcutPreference`/`VoiceInkShortcutStoragePreference`/`VoiceInkShortcutBackupPolicy`, with AppKit/Carbon shortcut decoding/materialization, validation checks/conflict lookup, event monitoring, permission handling/settings opening, notification delivery, dynamic Power Mode name lookup, `ShortcutBackup` records, and settings/recorder rendering remaining in the macOS shell.
57. macOS enhancement settings outer chrome, labels, shortcut row copy/key-chip labels, short-transcript threshold options, timeout options, and timeout retry labels route through `VoiceInkEnhancementSettingsPresentation`, with settings bindings and enhancement execution remaining in the macOS shell.
58. macOS audio-input storage keys, selected-device UID persistence, last-used microphone notification suppression, priority-device JSON storage, audio-device change/switch notification contract, settings labels, status copy, empty states, action icons, priority display text, and safe automatic input selection route through `VoiceInkAudioInputPreference`/`VoiceInkMacOSAudioDeviceChangeRequest`/`VoiceInkMacOSAudioInputSettingsPresentation`/`VoiceInkAudioInputAutomaticSelectionPolicy`, with CoreAudio device discovery, transport/UID adaptation, and selection side effects remaining in the macOS shell.
59. macOS Local CLI template identity, settings labels/help, command template catalog, command/selected-template/timeout storage, timeout default/options/clamp, configured-command predicate, full-prompt wrapper, stdout/stderr cleanup, command-failure classification, and execution error copy route through `VoiceInkLocalCLITemplate`/`VoiceInkLocalCLIPreference`/`VoiceInkLocalCLIExecutionError`, with shell execution remaining in the macOS adapter.
60. macOS local Whisper output-format settings labels/help/actions route through `VoiceInkLocalWhisperPromptCatalog`, with prompt object ownership and SwiftUI rendering remaining in the macOS shell instead of the model manager.
61. macOS model-performance panel copy/icons plus time filter labels, storage key/default, stored-value repair, and date-window policy route through `VoiceInkPerformancePresentation`/`VoiceInkPerformanceTimeFilter`, with SwiftData predicates and panel rendering remaining in the macOS shell.
62. macOS rolling-buffer preload settings labels/help, partial-transcript preview notification contract, VAD model labels, storage key/default, selected-model fallback, Silero predicate, service-route provider classification, backup payload shape, backup export planning, and backup-import validation/clamping route through `VoiceInkRollingBufferPreloadSettings`/`VoiceInkRollingBufferPreloadPartialTranscriptRequest`/`VoiceInkRollingBufferVADModel`/`VoiceInkRollingBufferVADSettings`/`VoiceInkRollingBufferBackupPreferences`/`VoiceInkRollingBufferBackupImportPlan`, with VAD execution, settings-change notification posting, and settings rendering remaining in the macOS adapter.
63. macOS recorder style labels, settings section/picker labels, raw storage key/default, visible-recorder predicate, window-kind mapping, and launch-at-login/menu-bar-only/recorder-style backup planning route through `VoiceInkRecorderStyle`/`VoiceInkRecorderStylePreference`/`VoiceInkMacOSShellBackupPreference`, with NSWindow/AppKit panel creation and launch/menu side effects remaining in the macOS adapter.
64. macOS general settings backup preference grouping, sub-plan routing, and portable preference writes route through `VoiceInkGeneralSettingsBackupPolicy`, with `GeneralBackup` preserving the macOS JSON wire fields and `BackupImporter` preserving shortcut, launch/menu/recorder, live feedback-controller, notification, and persistence side effects in the macOS adapter.
65. macOS Power Mode emoji catalog, custom emoji storage key, input validation, duplicate detection, add/remove mutation policy, picker input-draft feedback, submit-result feedback, picker action/icon/help copy, feedback severity, and in-use deletion alert presentation route through `VoiceInkPowerModeEmojiCatalog`/`VoiceInkPowerModeEmojiInputPresentation`, with SwiftUI picker rendering, usage lookup, and alert execution remaining in the macOS adapter.
66. macOS Power Mode settings row, disable-alert copy, panel, sidebar empty state, reorder sheet, badges, row actions, manual-selection popover copy, panel help URL, and panel/reorder/sidebar/form/app-picker/popover/row trigger/action symbols route through `VoiceInkPowerModePresentation`, with SwiftUI layout, settings bindings, panel/popover presentation, drag/reorder execution, and selection actions remaining in the macOS adapter.
67. macOS Power Mode config form seed defaults, trigger-section labels, transcription section labels, AI toggle title, footer actions, website add planning, and trigger removal route through `VoiceInkPowerModeConfigurationFormState`/`VoiceInkPowerModePresentation`/`VoiceInkPowerModePolicy`, with SwiftUI layout, AppKit app discovery/icons, state assignment, save/delete dispatch, and field focus remaining in the macOS adapter.
68. macOS Power Mode app picker search copy and app-trigger toggle policy route through `VoiceInkPowerModePresentation`/`VoiceInkPowerModeAppConfig`, with NSWorkspace app discovery, app icons, popover layout, and search filtering remaining in the macOS adapter.
69. macOS license startup, diagnostics, Polar adapter, secure-storage adapter, and license-management presentation route non-sensitive license preference keys, license startup/trial lifecycle, validation feedback/application planning, purchase/management URL policy, support-link metadata, activation/deactivation copy, trial-banner presentation, feature copy/icons, removal/reset planning, activation-required storage, first-launch storage, activation-limit storage, fallback device-id storage, stored-license access predicates, license Keychain account names, trial-start timestamp coding, device-local syncability, Polar endpoint/request DTOs, granted/activation-required response policy, and HTTP error mapping through `VoiceInkLicensePreference`/`VoiceInkLicenseStartupPolicy`/`VoiceInkLicenseValidationPolicy`/`VoiceInkLicenseLinks`/`VoiceInkLicenseManagementPresentation`/`VoiceInkLicenseTrialBannerPresentation`/`VoiceInkLicenseRemovalPolicy`/`VoiceInkLicenseSecureStorageAccount`/`VoiceInkLicenseSecureStoragePolicy`/`VoiceInkLicenseServicePolicy`, with Keychain execution, serial-number lookup, `URLSession`, AppKit URL opening, notification posting, SwiftUI colors/layout, and UI state remaining in the macOS shell. The obsolete raw-copy `LicenseView.swift` stays deleted so license UI enters through the shared-presentation `LicenseManagementView`.
70. A real Xcode toolchain is selected and both app targets build.

Current full-build blockers: `xcode-select -p` points to `/Library/Developer/CommandLineTools`, no `Xcode.app` is visible under `/Applications` or `/Volumes`, `/Users/atalphalnmomhappyhouse/VoiceInk-Dependencies/whisper.cpp/build-apple/whisper.xcframework` is missing, and the iphonesimulator SDK is unavailable. Full target builds are still environment-blocked until a real Xcode is selected, the shared Whisper framework exists, and the iOS simulator platform is installed. Until those are present, `scripts/verify-ios-single-repo-migration.sh` is the local proof gate; `--full-build` preflights those prerequisites before running app builds, and when SwiftPM is blocked by `sandbox-exec`, the script builds and runs `VoiceInkCoreChecks` and `VoiceInkAudioProof` through direct `swiftc` fallback paths.
